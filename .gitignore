# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# package manager (keep lock files for CI reproducibility)

# semantic-release
.nyc_output

# env files (can opt-in for committing if needed)
.env*
!.env.example

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

# flutter
.dart_tool
build
GoogleService-Info.plist

repomix-output.xml
.serena/cache

# Internal planning (not for public)
plans/
!plans/templates/*
screenshots/*
docs/screenshots/*
docs/journals/*
docs/research/*
logs.txt
test-ck
__pycache__
prompt.md

# Gemini CLI settings (symlink to .claude/.mcp.json)
.gemini/settings.json
dist
.mcp.json
.cache
data/**

# Translation cache
.translation-cache.json

# Local temp files
.tmp/

# Client data (private — never push to shared repo)
clients/*/
!clients/_template/
!clients/_smoketest/
!clients/_smoketest2/
!clients/README.md

# Voice profiles (private)
voice/*/
!voice/README.md

# Client-specific content (generated assets with real client data)
docs/content/

# Export zips (generated by scripts/export.py)
exports/

# Credentials
credentials.json
token.json

# Local-only Claude Code files (never push)
.claude/settings.local.json
.claude/hooks/
.claude/projects/
.claude/scripts/
.claude/telegram/

# ClaudeClaw personal files (operator-specific, never push)
SOUL.md
USER.md
CHANNELS.md
cron-registry.json
credentials/
.agent/
.agents/
.claude/scheduled_tasks.lock
scripts/modal/credentials.json
scripts/modal/*-credentials.json
skills/design-system/node_modules
skills/design-system/package-lock.json

# Video/image asset binaries downloaded during ad ingestion — local-only working
# copies used for transcription because Meta CDN URLs expire. DB keeps the URL;
# binaries don't belong in git.
swipe-files/**/ads/assets/
swipe-files/**/assets/

# embedded git repo at root — canonical-location decision pending (rebuild M4.5, see AUDIT-REPORT.md)
/propwise-sg/
