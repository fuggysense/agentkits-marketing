# DCT3 spotter run — canonical copy + duplicate marker

**Apply to:** `clients/neezanizam/campaigns/buyer-funnel/angles/_spotter-runs/wave-1/DCT3/_source.md`

(Staged here because neezanizam is a live client and this session treats it read-only.)

---

This DCT3 angle-spotter run exists in two places:

- **Canonical (this folder):** `clients/neezanizam/campaigns/buyer-funnel/angles/_spotter-runs/wave-1/DCT3/`
  16 files, newest mtime 2026-05-31. This is the authoritative copy — edits land here.
- **Original run output (duplicate):** `~/AI workflows/big-angle-spotter/runs/neezanizam_DCT3_260421-1748/`
  22 files, newest mtime 2026-04-21. The raw spotter run-dir the pipeline wrote first.

The client copy is the one downstream work reads. The `~/AI workflows` copy is kept as the run archive.

**No deletion performed.** Removing the `~/AI workflows` original needs operator OK (same handling as the eugene `RELOCATED.md` pattern). This marker just records which copy is canonical so nobody edits the stale one or assumes the duplicate is authoritative.

Marked during rebuild M3.7 / M4.5 (E-08).
