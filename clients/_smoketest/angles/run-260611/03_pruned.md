# Step 03 — prune against EXISTING_ANGLES (pass-through)

> FICTIONAL SMOKE-TEST DATA - not a real client. EMULATED.

EXISTING_ANGLES = "(none — treat Step 3 as a pass-through)". Per the pipeline contract, step 03
prunes the generated set against angles already tried. Nothing to prune. The 8 banked winners from
the gate carry through unchanged.

In hardened mode the saturation check is already enforced inside the gate via the `not_saturated`
dimension scored against `kill-list.md`, so step 03 is doubly a no-op here: A09 was already failed at
the gate for edging the kill-list's anti-urgency theme.

Carried forward: A01, A02, A03, A04, A05, A06, A08, A10.
