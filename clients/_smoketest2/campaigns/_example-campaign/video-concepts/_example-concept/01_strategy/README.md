# 01_strategy/

**What lives here:** strategy artifacts that shape concept generation — primarily `creative-diversity-map.json` (lanes, hook families, angle coverage matrix).

**Load to do X:**
- Generate AG1 concepts → load this folder + `../concept-brief.json` + `skills/video-concept-lab/SKILL.md`.

**Owner agent:** `creative-diversity-map` (strategy only — does NOT generate concepts).

**Phase gate:** outputs of this folder feed `phase_2_ag1_options`. Diversity map should be ready before `video-concept-seeder` runs.
