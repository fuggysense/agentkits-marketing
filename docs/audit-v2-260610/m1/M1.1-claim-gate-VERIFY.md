# M1.1-claim-gate — Acceptance Verification (adversarial)

Verifier: independent acceptance agent (did not build). Branch `rebuild-v2`. Date 2026-06-11.
All checks read-only outside the builder's own files. No network / Meta / sheet / render calls.

## Verdict: CONFIRMED

Every acceptance criterion reproduced from scratch with my own commands.

## Reproduced tests

### Test 1 — `--audit` Eugene dct-002 (read-only)
`python3 scripts/claim_gate.py --audit clients/eugene-chieng/campaigns/upgrader-ads/dcts/dct-002-math-blind/dct.json`
- `claims found: 24   unsourced: 2`
- The 2 unsourced are exactly the two `$214,300` occurrences:
  - `dct-image:DCT002-img-03.image_prompt` (dct.json:160) — UNSOURCED
  - `dct-image:DCT002-img-04.image_prompt` (dct.json:173) — UNSOURCED
- The `$214,300` in `img-03.visual_style` (dct.json:159) is correctly NOT counted — its sentence carries "illustrative set dressing ... explicitly fictional" and is stripped by `_strip_disclaimer_spans`. Verified the image_prompt occurrence DOES survive the strip ("Outstanding loan balance: $214,300 (down from last year)" has no disclaimer marker), so the flag fires for the right reason.
- A01 `2.5%` traces to `~/AI workflows/research-vault/.../linkup_results.ndjson` — genuine match ("accrued interest at 2.5% per annum compounded"), grep-confirmed. Not a coincidence.
- Eugene folder `git status` clean before and after. Never edited.

### Test 2 — `--gate` smoke dct
`python3 scripts/claim_gate.py --gate clients/_smoketest/campaigns/wave-smoke-260611/dct.json`
- Exit 1. `CLAIM GATE — FAIL (3 unsourced of 27 claims)`.
- All 3 unsourced are PD-IMG-01 = `73%` in DCT-SMOKE-01-img-03, across `text_on_image_hook`, `image_prompt`, `visual_style`. Named in plain language with the three fixes.
- Ledger does REAL work (not decorative): simulated stripping the `claims:` ledger → `S$900k`, `S$1.6m`, `S$12k` revert to UNSOURCED. With the ledger present they resolve. `S$290`/`S$4,500`/`S$30k` resolve via offer whitelist (all present in `_brand/offer.md` L13/L18/L19). So "only PD-IMG-01 remains failing" is a true consequence of the added sourcing, not of there being no other claims.

### Structural / shape coverage
- Legacy `creatives[]`: synthetic fixture → extracts `$88,000` (traces) + `47%` (UNSOURCED). Works.
- `--no-trace`: Eugene goes 2 → 22 unsourced (auto-trace disabled). Flag works.
- False-positive control: fixture with year `2024`, dimensions `1080x1080`, layout `top 28% / 92% opacity`, and disclaimer-wrapped `$50,000` → 0 claims extracted. All suppressed as specified.
- Pass path: ledger-sourced `99%` → `CLAIM GATE — PASS`, exit 0.

### Two modes
- `--audit`: claim|where|source-status table, exit 0 always. Confirmed.
- `--gate`: exit 1 on any unsourced with per-claim plain-language fix (add source / reword / cut), exit 0 on PASS. Confirmed.

### Source resolution order
ledger → offer.md price whitelist → auto-trace (client research dirs + research-vault). Confirmed live in the smoke audit table (ledger / offer / traced / UNSOURCED all appear with correct precedence).

### WIRE — ACE SKILL.md
- `### Claim Gate (machine precondition — runs BEFORE HITL Gate 3)` at line 618, sits between Phase 3b (606) and HITL Gate 3 (644).
- Includes command, exit-code semantics, a failure example matching real output, the three fixes, and a never-bypass-silently rule (line 642).
- HITL Gate 3 (line 646) updated: do not enter until Claim Gate returns exit 0 or carries a recorded override. Reads sanely.

### Writing anti-AI check (subsection 618-646)
- 0 AI-tell words (delve/leverage/robust/etc.). 2 em-dashes (under 3). 0 curly quotes. Concrete command + file names. Pass.

### Hard rules
- stdlib only (argparse, json, os, re, sys). Zero network/subprocess/Meta/sheet/render. `py_compile` clean.
- research-vault: no files modified after 2026-06-10. Read-only honored.
- Eugene: git-clean. Only writable files touched: `scripts/claim_gate.py`, smoke `dct.json`, ACE `SKILL.md`, this report dir.

## Notes (not blocking; logged, not fixed per surgical rule)
1. Auto-trace cites the FIRST file containing the number, which for 4+ digit currency can be a coincidental same-string file rather than the canonical source (builder's logged weakness (a)). Verdict stays correct; the cited PATH may be incidental. The ledger is authoritative when precision matters.
2. Percentages CAN resolve via auto-trace (2.5% does). A stricter "ledger-only for percentages" policy is a one-line change in `resolve_source` if the team prefers. Left as-is — forcing a genuinely attested rate to UNSOURCED would be a false positive. This is an operator policy call, not a bug.
3. Out-of-scope observation: a `~/AI workflows/big-angle-spotter/scripts/run_pipeline.py.bak-260611` exists — belongs to a different task (big-angle-spotter), not M1.1. No action.
4. The `$88,000` legacy fixture traced even with no client root because research-vault is always searched. Correct behavior; same as weakness (1).
