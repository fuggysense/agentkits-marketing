# M1.2 — Persona-Quote Provenance — Task Report

> Date: 260611 · Branch: rebuild-v2 · Scope: add quote-provenance discipline to avatar-research + flag the neezanizam unsourced-quote incident. No network, no sheet writes, no Meta, no render. neezanizam left READ-ONLY.

## Task 1 — avatar-research Quote Provenance hard rule

**File changed:** `skills/avatar-research/SKILL.md`

Before writing any prose, read `/Users/jerel/.claude/commands/writing/SKILL.md` and applied the anti-AI rules + self-check to all added instruction text (straight quotes, em-dashes minimized, no AI-tell words, named files/lines instead of "experts", one negative-parallelism max, no summary paragraph).

Edits (5 surgical changes):

1. **Line ~382** (Micro-Persona Map template, Language field) — appended "every verbatim phrase needs an inline source pointer, see Quote Provenance below".
2. **Line ~393** (Psychology block, Raw inner dialogue) — now requires an inline source pointer; inferred self-talk with no source must be tagged `[HYPOTHESIS - not customer language]`.
3. **Lines 406-434** (new `#### Quote Provenance (HARD RULE — no source, no quote)` block, inserted after the Market Behavior template, before Quality checks) — the load-bearing rule:
   - Every quote/verbatim pattern written to `buyer-profile.md` or any `_brand/avatars/*.md` MUST carry a source pointer (relative path + line/anchor to a research file, transcript, or research-vault doc).
   - No source = REFUSE to write. Two paths offered: (1) find a real quote and cite it, or (2) tag `[HYPOTHESIS - not customer language]` which downstream skills (ad-concept-engine, headline-bank, copywriting, big-angle-spotter) MUST NOT quote in ads.
   - Bare platform names ("Grok", "from Reddit") and "synthesized from research" are explicitly disallowed as pointers.
   - Worked example uses the smoke client: VOC quote at `clients/_smoketest/00_inputs/research/voc-reddit-dump-260611.md:47` ("i'd pay a flat fee just to have one person look at my shortlist...") written WITH the pointer; buyer-truth traces to line 114; a non-existent "peace of mind" line shown as the `[HYPOTHESIS]` case. The "mental burden off my shoulders" neezanizam line is named as the real-world propagation cautionary tale.
4. **Line ~436** (Quality checks during compilation) — added provenance as the FIRST check: an unsourced quote blocks the write.
5. **Phase 4 Save (lines ~466, ~477)** — fixed the refresh-log "Sources" example from the anti-pattern "Perplexity, Grok, ChatGPT" to real file:line pointers; added step 5 "Provenance gate before save" (scan every quote pre-save, refuse if bare; applies to refreshes inheriting legacy unsourced quotes). Renumbered subsequent steps (now 1-8, verified no duplicate).

**Tests run:**
- `grep -nE "^[0-9]+\. "` on Phase 4 → steps 1-8, no duplicate numbering. PASS.
- `sed -n '47p'` VOC dump → matches the cited flat-fee quote verbatim. PASS.
- `sed -n '114p'` VOC dump → matches the cited buyer-truth line. PASS.
- `grep "peace of mind about the biggest purchase"` VOC dump → NONE, confirming the `[HYPOTHESIS]` example is correctly a non-quote. PASS.
- `git status --short skills/avatar-research/SKILL.md` → ` M` (modified, tracked). PASS.

## Task 2 — neezanizam quote-flag handoff

**File created:** `_handoffs/neezanizam-quote-flag-260611.md` (no neezanizam file touched)

Confirmed the phrase "I want this mental burden off my shoulders" in `clients/neezanizam/_brand/avatars/avatar-1.md` lines 11 and 272 has ZERO hits in any research input/swipe/transcript:
- Exact `mental burden off my shoulders` → 11 files, all derived from the avatar (1 origin + 10 downstream).
- Looser `burden off my shoulders` in `_swipe/` → NONE.
- avatar-1.md's own Research Sources block names only platforms (Perplexity/Grok/ChatGPT, compiled 2026-04-07) — no openable citation.

Documented every live artifact it propagated into (file + line + buyer-facing flag):
- ORIGIN: `_brand/avatars/avatar-1.md:11,272`
- `campaigns/dashboard.html:222` (published, buyer-facing)
- `campaigns/thomson-reserve/02_creatives/gpt-image-2-260531/_pitch-source-brief.md:96`
- `campaigns/buyer-funnel/dcts/dct-10-5-5-proof-260603/dct.json:23` + `dct-tracker.json:32`
- `campaigns/_sheet-snapshots/260603-1746-...json:42`
- spotter wave-1 DCT1 + DCT3 `system_prompt.txt:19,280` and `inputs.json:4` (avatar embedded wholesale)
- `campaigns/buyer-funnel/_drafts/dct-260408/chatgpt-talking-head-scripts-prompt.md:45`

Laid out the operator's two options: (A) capture a real source and update buyer-facing surfaces; (B) reword/tag as `[HYPOTHESIS]` + re-render affected assets (dashboard, pitch brief minimum). Recommendation included but framed as operator's call.

**Tests run:**
- `git status --short clients/neezanizam/` → EMPTY. neezanizam untouched. PASS.
- `grep -rln "mental burden"` neezanizam → 11 files, matches the table in the handoff. PASS.

## Acceptance

| Criterion | Status | Evidence |
|-----------|--------|----------|
| SKILL.md requires source pointer on every quote; no source = refuse | PASS | Quote Provenance block lines 406-421 + Phase 4 gate step 5 |
| Refuse-to-write offers find-real-quote OR `[HYPOTHESIS - not customer language]` | PASS | SKILL.md lines 416-421; downstream no-quote rule for hypotheses |
| Rule placed in output spec + one worked example using smoke client VOC line | PASS | Worked example lines 423-434, sourced to voc-reddit-dump-260611.md:47 (verified verbatim) |
| Writing anti-AI rules applied to SKILL.md prose | PASS | Read writing/SKILL.md first; self-check applied |
| neezanizam handoff lists phrase + every propagated artifact + 2 options | PASS | `_handoffs/neezanizam-quote-flag-260611.md` |
| No neezanizam file edited | PASS | `git status` clean for clients/neezanizam/ |
| No network / sheet / Meta / render | PASS | grep + sed + git only |

## Notes / things noticed (NOT fixed — out of scope)

- The spotter `inputs.json` / `system_prompt.txt` files embed avatar-1.md wholesale, so the same unsourced quote rides into every spotter run unfiltered. If the provenance rule is later enforced at the spotter input layer too, that would close the propagation path; flagged, not fixed.
- avatar-1.md has a separate, longer "Characteristic Quote" (~line 295) also lacking a pointer — same provenance gap, different line. Not in this task's scope (task named only the "mental burden" phrase); logged here.
- Whether any spotter run or the talking-head prompt actually produced buyer-facing ad copy carrying the quote was not traced — would need a pass through campaign output/review folders.
