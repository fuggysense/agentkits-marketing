# M1.3 — render.py repoint to current dct.json shape

**Task:** Repoint `scripts/ad-images/render.py` `--from-tracker` to the current `image_pool.images[]` dct.json shape, keep legacy parser, add claim-gate hook, fix stale paths, verify by byte-identical dry-run against the Eugene DCT002 sidecars.
**Date:** 2026-06-11 (SGT) • **Branch:** rebuild-v2 • **Render spend:** $0 (dry-run only, verified no PNGs written)

## What changed

### `scripts/ad-images/render.py` (rewritten, surgical to the named scope)
- **L45** — added `CLAIM_GATE` constant pointing at `scripts/claim_gate.py`.
- **L81–104 `run_claim_gate()`** — claim-gate hook. If `claim_gate.py` exists, runs it in `--gate` mode against the dct.json before rendering; non-zero exit blocks the render with a fix-or-`--skip-claim-gate` message. NO-OPs gracefully (stderr warning, `{status: absent}`) when the script is absent. `--skip-claim-gate` opts out with a logged warning (`{status: skipped}`).
- **L106–124 `detect_shape()`** — auto-detect: `image_pool.images[]` present → `current`, else `legacy`. `--legacy-shape` forces legacy.
- **L128–160 `resolve_current_shape()`** — resolves `(id, prompt)` from `image_pool.images[]`. Selection precedence: `--image <id>` (one image) → `--batch <angle_id>` (+ optional `--variant` filtering `variant_id`) → whole pool. Reads inline `image_prompt`, falls back to `image_prompt_file` reference.
- **L162–188 `resolve_legacy_shape()`** — the old `creatives[]`/`variations[]` parser, preserved verbatim in logic (inline `image_prompt` + `image_prompt_file` fallback; `--variant` matches `variant_id` or `variant`).
- **L190–198 `out_path_for()`** — current shape auto-targets `<dct-dir>/images/<id>.png` (matches where the pool already references files); legacy keeps `image-prompts/renders/<batch>-<variant>.png`.
- **CLI (L206–218)** — new flags: `--image`, `--legacy-shape`, `--skip-claim-gate`, `--confirm-all`. `--batch`/`--variant` kept (semantics adapt per shape); `--dry-run`, `--prompt`, `--out`, `--style`, `--engine`, `--size`, `--quality`, `--ref` unchanged.
- **L243** — claim gate runs once per `--from-tracker` invocation, before any resolution/render.
- **L259–261** — guards: `--out` rejected with multi-image selection; real render of >1 image refuses without `--confirm-all` (anti mass-spend). Dry-run is never gated.
- **L290–315** — dry-run now loops jobs, prints per-id resolved prompt + the exact `would run` command.
- Docstring examples (L17–30) repointed from the dead `dct-tracker.json` example to the live Eugene `dct.json` + legacy note.

### `scripts/ad-images/README.md`
- "Run it from a real pipeline output" section rewritten: current `image_pool.images[]` shape examples (`--image`, whole-pool, `--batch`/`--variant`), legacy `--legacy-shape` block, claim-gate paragraph. Replaced the stale `clients/<client>/campaigns/<slug>/dct-tracker.json` example path that no longer exists.

### Test fixture (writable dir, per task rules)
- `clients/_smoketest/m13-legacy/dct-tracker.json` — synthetic legacy `creatives[]`/`variations[]` fixture for the legacy-path regression test.

## Tests run (real output)

### Acceptance — 10 prompts dry-run resolve + byte-diff vs shipped sidecars
Drove the real CLI (`render.py --from-tracker <Eugene dct.json> --image <id> --dry-run --skip-claim-gate`) per pool image, extracted the resolved prompt block from stdout, diffed against each `images/DCT002-img-NN.png.meta.json` `prompt`:

```
DCT002-img-01: BYTE-IDENTICAL  (resolved=2891 chars, sidecar=2891 chars)
DCT002-img-02: BYTE-IDENTICAL  (resolved=1528 chars, sidecar=1528 chars)
DCT002-img-03: BYTE-IDENTICAL  (resolved=3540 chars, sidecar=3540 chars)
DCT002-img-04: BYTE-IDENTICAL  (resolved=1934 chars, sidecar=1934 chars)
DCT002-img-05: BYTE-IDENTICAL  (resolved=3551 chars, sidecar=3551 chars)
DCT002-img-06: BYTE-IDENTICAL  (resolved=2271 chars, sidecar=2271 chars)
DCT002-img-07: BYTE-IDENTICAL  (resolved=2268 chars, sidecar=2268 chars)
DCT002-img-08: BYTE-IDENTICAL  (resolved=1542 chars, sidecar=1542 chars)
DCT002-img-09: BYTE-IDENTICAL  (resolved=1911 chars, sidecar=1911 chars)
DCT002-img-10: BYTE-IDENTICAL  (resolved=1784 chars, sidecar=1784 chars)

10/10 byte-identical
```

### Whole-pool dry-run (no selector) → resolves all 10
`render.py --from-tracker <dct.json> --style dr-clean-static --dry-run` → `shape: current`, `jobs: 10`, ids DCT002-img-01..10. Claim-gate warning on stderr.

### Legacy path (synthetic fixture)
- auto-detect (no flag): `shape: legacy`, resolves `LEGACY PROMPT A01 v1 ...`, out `clients/_smoketest/m13-legacy/image-prompts/renders/DCT010-A01-v1.png`.
- `--variant v2` via the `variant` (not `variant_id`) key → `LEGACY PROMPT A01 v2 ...` (alias preserved).
- `--legacy-shape` force flag → `shape: legacy`.

### Current-shape filtering
- `--batch A02` → `jobs: 2` (img-01, img-02).
- `--batch A02 --variant v1` → `jobs: 1` (img-01).

### Claim-gate hook
- **Absent** (patched CLAIM_GATE to a nonexistent path): warns `claim gate not found ... skipping (built in parallel)`, returns `{status: absent}` — NO-OP graceful.
- **Skip flag**: warns `claim gate SKIPPED`, returns `{status: skipped}`.
- **Present + buggy (live)**: `scripts/claim_gate.py` landed mid-task (parallel deliverable) and currently crashes with `TypeError: 'NoneType' object is not iterable` (its `extract_claims_from_text` → `emit` returns None). My hook correctly treated the non-zero exit as a gate failure and BLOCKED the render — exactly the intended fail-closed behavior. Acceptance was therefore run with `--skip-claim-gate` (per the task's documented opt-out). See "Noticed, not fixed" below.

### Safety
- `--confirm-all` gate: real render of the 10-image pool without it → refuses, `exit=1`, zero spend.
- `git status` after all testing: no new/modified `.png` files anywhere. Dry-run only; no network/Meta/sheet/render calls.

## Acceptance status

| Criterion | Status | Evidence |
|---|---|---|
| `--from-tracker` resolves current `image_pool.images[]` + `image_prompt` | PASS | whole-pool dry-run = 10 jobs, ids resolved |
| Selection args adapt (`--image`, `--batch`/`--variant`), backward-compatible | PASS | filtering tests above |
| Legacy `creatives[]`/`variations[]` behind `--legacy-shape` (also auto-detected) | PASS | legacy fixture tests |
| Claim-gate hook in `--gate`, `--skip-claim-gate` opt-out, NO-OP if absent | PASS | absent/skip/present-buggy branches |
| Stale docstring + README dct-tracker.json path fixed | PASS | render.py L17–30, README rewritten |
| `--dry-run` prints resolved prompts, no render | PASS | all tests dry-run, no PNGs written |
| **Eugene dct.json dry-run resolves all 10; 10/10 byte-identical vs sidecars** | **PASS** | 10/10 byte-identical |

## Noticed, not fixed (out of M1.3 scope — logged per task rules)
1. **`scripts/claim_gate.py` is buggy.** It crashes on the Eugene dct.json with `TypeError: 'NoneType' object is not iterable` at `extract_claims_from_text` (line ~153, the `emit(...)` generator returns `None`). It is a parallel deliverable (another M1 task), not mine to fix. Flagging for whoever owns the claim-gate task. My hook handles it correctly (fail-closed; override is `--skip-claim-gate`).
2. **Sidecar schema change.** New renders write `shape` + `key` fields and drop the old `batch`/`variant` fields. The 10 shipped DCT002 sidecars predate this and still carry `batch:null, variant:null, source:"inline-prompt"` — they were hand-rendered via `--prompt`, not `--from-tracker`. The prompt text (the load-bearing field for this task) matches byte-for-byte regardless. Not re-rendering (would spend credits + mutate a live-client folder); noted for awareness only.
3. **dct.json `launch_gates[]` last item** still says "render.py --from-tracker ... read[s] legacy dct-tracker.json shapes, neither consumes dct.json directly (pipeline-state blockers G1/G4)." That note is now stale (render.py consumes dct.json directly). The dct.json is a live Eugene client file (READ-ONLY) — did not edit; flagging so the operator can clear the blocker note.
```
