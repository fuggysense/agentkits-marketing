# M1.3-render-repoint — Acceptance Verification (adversarial)

Verifier did NOT build this. All commands below were re-run independently. Date: 2026-06-11.

## Verdict: CONFIRMED

Every acceptance criterion reproduced. One minor non-blocking hygiene bug found and logged (not fixed, per scope).

## Files reviewed
- `scripts/ad-images/render.py` (modified — 200+/54− vs git)
- `scripts/ad-images/README.md` (modified)
- `clients/_smoketest/m13-legacy/dct-tracker.json` (new fixture)
- `clients/eugene-chieng/.../dct-002-math-blind/dct.json` (READ-ONLY, current-shape source)

## Criterion-by-criterion (reproduced)

1. **--from-tracker resolves current shape + selection args** — PASS.
   Whole pool: `shape:current jobs:10` (ids DCT002-img-01..10). `--image DCT002-img-01` = 1 job, out → `.../images/DCT002-img-01.png`. `--batch A02` = 2 jobs (img-01,img-02). `--batch A02 --variant v1` = 1 job. `--prompt/--out/--style/--engine/--list-*` backward-compatible (inline mode shape:inline jobs:1; `--prompt` w/o `--out` errors correctly).

2. **Legacy parser behind --legacy-shape + auto-detect** — PASS.
   Auto-detect on fixture → `shape:legacy`, resolves A01 v1 prompt, out → `image-prompts/renders/DCT010-A01-v1.png`. The `variant` key alias (v2) resolves alongside `variant_id` (v1). `--legacy-shape` force flag → `shape:legacy`.

3. **Claim-gate hook** — PASS.
   - Gate FAIL on real Eugene file → render.py exits **1** (fail-closed, verified with real exit code, not piped).
   - `--skip-claim-gate` → stderr warning, `{status:skipped}`.
   - Absent branch (monkeypatched CLAIM_GATE to /tmp nonexistent) → warning, `{status:absent}`.
   - **CORRECTION to builder's note:** builder claimed claim_gate.py crashes with `TypeError: 'NoneType' not iterable`. It does NOT anymore — `claim_gate.py` (20761 bytes, parallel deliverable) now runs cleanly, returns a proper FAIL verdict (2 unsourced "$214,300" currency claims), exit 1, no Python traceback. The parallel task fixed it after the builder wrote the report. render.py's hook is correct either way.

4. **Stale paths fixed in docstring + README** — PASS.
   render.py docstring L21-30: current-shape Eugene example + legacy note. README L34-62 rewritten with `image_pool.images[]` examples; no dead `clients/<client>/campaigns/<slug>/dct-tracker.json` example path. `dct-tracker.json` now appears only in legacy context.

5. **No render / no spend / no network / no sheet / no Meta** — PASS.
   All tests dry-run. `git status` shows zero new/modified `.png`. Whole-pool real render refused without `--confirm-all` (exit 1). No external calls.

6. **10 Eugene prompts byte-identical to sidecars** — PASS (this is the load-bearing one, verified two ways).
   - Direct: pool `image_prompt[i]` vs `images/<id>.png.meta.json.prompt` → 10/10 SHA-256 match, char counts 2891,1528,3540,1934,3551,2271,2268,1542,1911,1784 (match builder's claim exactly).
   - **Via the real CLI path** (`render.py --from-tracker ... --image <id> --dry-run`, parsed the printed prompt block): 10/10 cli_len == pool_len, no off-by-one / trailing-whitespace divergence. This is stronger than the builder's evidence — confirms the resolver, not just the JSON field.

## Flagged, NOT fixed (out of scope — surgical mandate)

1. **Dry-run filesystem side-effect (minor hygiene bug, in render.py itself).** `out.parent.mkdir(parents=True, exist_ok=True)` at render.py:271 runs UNCONDITIONALLY before the dry-run check at :275. A legacy dry-run created an empty, untracked `clients/_smoketest/m13-legacy/image-prompts/renders/` directory. Verified: the dir is empty (0 files) and untracked — pure dry-run artifact, not part of the committed fixture. Contradicts the docstring's "spend nothing" only in the weakest sense (creates empty dirs, no files, no credits). For Eugene single-image dry-runs `images/` already exists so it's a no-op there — protected folder NOT mutated in the tested path, but a dry-run against a fresh `--out` or a legacy tracker pointing at a new dir WILL mkdir empty dirs. Fix would be to move the mkdir inside the non-dry-run branch. LEFT for owner.

2. **Builder report's claim_gate "TypeError" note is now stale** (see criterion 3). Cosmetic — the builder correctly flagged it as a parallel deliverable; it's since fixed. No action on M1.3.

3. **Inherited from builder (confirmed, agree it's out of scope):** dct.json `launch_gates[]` still claims render.py can't consume dct.json — now stale, but dct.json is READ-ONLY Eugene; correctly left for operator.

## Protected-folder compliance
`git status` clean for eugene-chieng / neezanizam / takekine — no edits. Test fixture only in writable `_smoketest/`.
