# M1.4-sheet-writers — Acceptance Verification (adversarial)

Verifier run: 260611-0119 SGT · branch `rebuild-v2` · verifier did NOT build this.
Verdict: **CONFIRMED** — every acceptance criterion reproduced with my own read-only / dry-run commands. No live writes, no network, no Meta, no renders.

## Files reviewed (diffs read in full)
- `scripts/tr_10_5_5_sheet_writer.py` — generalized (+166 / restructured). Diff read.
- `scripts/source_of_truth_sheet_writer.py` — dual-path back-port only (~+15). Diff read.
- `scripts/patch_angle_cell.py` — dual-path back-port only (~+16). Diff read.
- Fixtures: `clients/_smoketest/_brand/metrics-config.fixture.json`, `clients/_smoketest/campaigns/tr-smoketest/dcts/DCT201|DCT202/dct.json`. Read.

## Criterion-by-criterion (reproduced)

**(a) neezanizam dry-run parity — CONFIRMED.**
`python3 scripts/tr_10_5_5_sheet_writer.py --client neezanizam --campaign thomson-reserve --metrics-campaign thomson-reserve-buyers --mode dry-run`
→ config resolved from `clients/neezanizam/_brand/metrics-config.json` (root absent, _brand/ hit — dual-path works); `sheet_id=1KqWJP08h8BPr8ygH1ADnmrDPuGaH3p7v4O3WS6WoAtM`; tabs CREATIVES/COPY; DCTs `['DCT101'..'DCT105']` discovered from folders; 16-col CREATIVES + 12-col 10-5-5 COPY previews; blank metric cols tagged; tail `READY: all 5 DCTs have full copy`. SA-fallback warning shows config SA == default-creds SA (`neezanizam@neezanizam-492212...`). `git status --porcelain clients/neezanizam` = EMPTY after run.

**(b) smoketest fixture dry-run — CONFIRMED.**
`... --client _smoketest --campaign tr-smoketest --config clients/_smoketest/_brand/metrics-config.fixture.json --mode dry-run`
→ `sheet_id=1FAKEsmoketestSHEETid...`; DCTs `['DCT201','DCT202']`; coherent CREATIVES+COPY previews; DCT202 = 4/5 copy → tail `HOLD`. SA-divergence warning loudly surfaced (`smoketest@meridian-fictional...` in config vs `neezanizam@...` in default creds).

**(c) imports of all three — CONFIRMED (with builder's stated nuance).**
- `python3 -m py_compile` all three → OK.
- tr writer: stdlib-only, direct exec import → `OK import tr_10_5_5`.
- The other two import `from modal.sheets_writer import SheetsWriter` at module top; `modal/sheets_writer.py` imports `gspread` + `google.oauth2` (lines 14-15). gspread is NOT installed in this env (`ModuleNotFoundError`). Confirmed pre-existing: `git show HEAD:` shows both sibling scripts already had that import on HEAD — NOT introduced by this task. With a minimal stdlib stub (no install, no network) both import cleanly → `OK import source_of_truth_sheet_writer`, `OK import patch_angle_cell`.
- Bonus: exercised the back-ported lookup live — `patch_angle.load_metrics_config("neezanizam")` and `source_of_truth._load_config()` both resolve the `_brand/` path; the not-found path lists BOTH candidate locations.

**(d) dry-run default + 5/5 gate (S-14) preserved — CONFIRMED.**
`--mode` default = `dry-run`. live_write ABORT line verbatim vs HEAD: `ABORT: {dct_id} copy not 5/5 ({filled}/{total}). Refusing live write.` (HEAD line 131 == new line 230, identical). dry-run HOLD/READY keyed on `filled==total`; verified HOLD on smoketest DCT202 and READY on neezanizam.

**(e) hardcoded SHEET_ID / DCT_IDS / thomson defaults removed — CONFIRMED.**
`grep` of generic execution path: no `DCT_IDS` array, no `SHEET_ID =` literal, no `--client/--campaign` TR defaults. Remaining "thomson"/"DCT101" mentions are docstring/comment ONLY (lines 3, 16, 18) — documentation, not execution constants. `--client`/`--campaign` now `required=True` (verified: running with neither errors `the following arguments are required: --client, --campaign`). Added `load_config` (dual-path + `--config`), `resolve_credentials` (per-client path else loud SA-naming warning), `discover_dct_ids` (folder scan). Bad `--metrics-campaign` errors with the available-slugs list.

**(f) dual-path back-ported to the two siblings — CONFIRMED.**
Both now search `[client_dir/metrics-config.json, client_dir/_brand/metrics-config.json]` first-hit-wins with a "looked in" error, identical to `ad_concept_sheet_writer.py:271-273` reference. Diffs confirm only that block changed in each.

**(g) no live writes / network / Meta / renders — CONFIRMED.**
All runs `--mode dry-run`. `live_write` + `_gws_update` (subprocess to gws) reachable ONLY via `args.mode == "live"` (call site line 282); never fired. `git status --porcelain` for `clients/eugene-chieng`, `clients/neezanizam`, `clients/takekine` all EMPTY after every run.

## Builder flags I independently verified (correctly out-of-scope, NOT fixed)
1. gspread not installed; siblings import it at module top via `modal.sheets_writer` — pre-existing on HEAD, not introduced. Builder's "make lazy like ad_concept_sheet_writer" suggestion is reasonable but out-of-scope.
2. Canonical `clients/_smoketest/_brand/metrics-config.json` template kept pristine (`sheet_id: {{GOOGLE_SHEET_ID}}`, `campaigns: []`, git-clean). The `if "sheet_id" not in raw` flatten guard treats the placeholder as a truthy top-level sheet_id and skips campaigns[] flatten — a real latent edge, but the SAME guard is in the reference `ad_concept_sheet_writer.py:287` and pre-existing `source_of_truth_sheet_writer.py:99`, so it's a faithful mirror, not a regression. Real clients omit top-level sheet_id; both neezanizam and the fixture exercise the flatten path correctly. Fixture correctly lives in a sibling `.fixture.json` loaded via `--config`.
3. TR live path uses gws CLI (human OAuth), not the SA creds; `resolve_credentials` is informational/parity for this writer today. Noted, not load-bearing for dry-run.

## Diffstat
tr_10_5_5: +166/restructured · source_of_truth: +15 · patch_angle: +16. Surgical, scoped to the task.
