# M1.5-provisioner-into — Acceptance Verification

Verifier: adversarial, did not build. Date 2026-06-11 (SGT).
File under test: `skills/sheets-provisioner/scripts/provision_from_template.py`
Baseline for diff: m0 snapshot `5e867e5`.

## Verdict: CONFIRMED

Every acceptance criterion reproduced independently. No network/live writes/renders. google python libs are NOT installed locally (confirmed: `import google.oauth2.credentials` fails), so the script cannot run live here — verified via `py_compile` + a from-scratch stubbed-module harness that records every mutating API call and monkeypatches `gcloud_token` (no gcloud/curl/network reached).

## Bug confirmed in baseline
`git show 5e867e5:...` — the `--into` branch ran `old_tab_ids = [s["properties"]["sheetId"] for s in dmeta["sheets"]]` (ALL pre-existing dest tabs), then `for sid in old_tab_ids: requests.append({"deleteSheet": ...})`. So an `--into` run deleted every operator tab in the destination. Real bug, real fix.

## Criterion-by-criterion (re-run with my own commands)

1. **Before/after diff of delete path** — PASS. `git diff 5e867e5`: `old_tab_ids` (collected all dest tabs) split into `placeholder_tab_ids` (L113 `[]` for --into; L135 created Sheet1 for --title) + `existing_tab_names` (L112, collision check only). Delete loop L175 now `for sid in placeholder_tab_ids`.

2. **Trace every branch to delete; --into can't reach it** — PASS. Exactly 3 assignments to `placeholder_tab_ids`: L113 `--into` = `[]`; L126 `--title`+`--dry-run` = placeholder str (but `return` at L157 fires before delete loop L175); L135 `--title` live = created Sheet1 only. `--into`/`--title` is `required=True` mutually-exclusive argparse group, so under `--into` only L113 is reachable → delete loop iterates an empty list.

3. **--dry-run --into shows append-only plan** — PASS. My harness T1 (dest has 3 tabs, adding CREATIVES+COPY): stdout `preexisting_tabs_to_delete: []`, `tabs_already_present: [DAILY CALLS,KPIs,APPTLOG]`; stderr `[dry-run] APPEND-ONLY: 2 tab(s) added, 3 pre-existing tab(s) left untouched, 0 deletes.`; recorded mutating calls = `[]`.

4. **Name collision fails loud, no overwrite/delete** — PASS. T2 (APPT in both source tabs and dest): exit = `REFUSING to provision: these tabs already exist in the --into target (no overwrite, no delete): ['APPT']...`; mutating calls = `[]`. Guard runs before any mutation.

5. **No live API/sheet writes/network** — PASS. py_compile OK; T1/T2/T5 recorded zero mutating calls; live paths exercised only against stubbed google modules.

## Extra adversarial tests I added (beyond builder's three)
- **T3 — live `--into`, no collision (the actual bug scenario, NOT dry-run):** `deleteSheet` requests in the batchUpdate = `[]`. Mutating call sequence = `copyTo, copyTo, batchUpdate, batchClear` — append + clear only, no delete. This is the strongest proof the destructive path is dead even on a live run.
- **T4 — live `--title` new workbook:** placeholder Sheet1 (sheetId=0) IS deleted (`[{'deleteSheet': {'sheetId': 0}}]`) — regression check that the legit cleanup still works.
- **T5 — `--title --dry-run`:** zero mutating calls (no `create`), plan shows the placeholder in `preexisting_tabs_to_delete`.

## Scope check
`git diff --name-only 5e867e5 -- skills/sheets-provisioner/` = only `provision_from_template.py`. Report dir untracked. `find_or_create_sheet.sh` does NOT reference the python provisioner (grep: no matches) — builder's claim it was not the bug source and was correctly left untouched is verified.

## Observations (not fixed — logging per hard rules)
- SKILL.md still likely describes `--into` as a reshape/clear op (builder noted lines 191/214). Stale doc wording now that `--into` is append-only — follow-up doc pass. (Not in scope of this code task.)
- D-06 (sibling sheet writers hardcoding a client SA key) untouched — separate finding.
- No Eugene/neezanizam/takekine folders read or written.
