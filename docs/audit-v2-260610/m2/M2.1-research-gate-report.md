# M2.1 — Niche-Adaptive Research-Completeness Gate — Report

Branch: rebuild-v2. Date: 2026-06-11 (SGT).
Task: gap row 2 (double-win) — a niche-parameterized "research complete" contract + a machine
gate that enforces it before any avatar/angle/ad work runs.

## State found on entry (resume, not greenfield)

Commit `0fff0f4 chore(rebuild): m2 partial wip — stopped mid-run on operator request` had already
landed most of this task. Four of the six owned deliverables existed and were correct:

- `clients/_template/_brand/research-brief.md` — niche-parameterized YAML + prose + `## Builder interview`. Present, complete (the "builder doc" is the `## Builder interview` section inside the same file).
- `scripts/research_gate.py` — stdlib-only gate, dual-resolution (client folders + research-vault), Ferres-floor fallback, `--json`. Present, working.
- `clients/_smoketest/_brand/research-brief.md` — property-advisory niche tuning. Present.
- Smoke fixtures: real research pack under `clients/_smoketest/00_inputs/research/` (PASS) and a thin fixture at `clients/_smoketest/_baseline/thin-fixture/` (FAIL). Present.

Two deliverables were missing and are what this run added:

1. The avatar-research precondition block (verbatim) — was NOT in the SKILL.md.
2. `_handoffs/research-calibration-interview.md` — did not exist.

## Changes (file:line)

### 1. skills/avatar-research/SKILL.md (+4 lines, inserted after line 57)
Added the canonical M2.1 precondition block verbatim into the Prerequisites (inputs) section,
between the Required-files list and `**Foundation flexibility:**`:

```
## Research-completeness precondition (rebuild M2.1)
Before generating for any client, run: python3 scripts/research_gate.py --client clients/<slug>
PASS is required. On FAIL: stop and surface the scorecard to the operator — never generate from thin research. Override only via an operator-recorded research_gate_override (with reason) in the campaign's pipeline-state.json
```

Now at `skills/avatar-research/SKILL.md:59-61`. M1 work confirmed intact and unweakened: Quote
Provenance rules still present (5 references, the `#### Quote Provenance (HARD RULE)` block and the
`Provenance gate before save` step at line ~481). Confirmed M1 Claim Gate in
`skills/ad-concept-engine/SKILL.md:618` (`### Claim Gate (machine precondition — runs BEFORE HITL
Gate 3)`) is also untouched.

### 2. _handoffs/research-calibration-interview.md (NEW, 123 lines, 12 questions)
The instrument the operator answers to tune his *standing* floor across the roster he serves
(SG property strategy, DTC wellness, coaching, agency lead-gen). Distinct from the per-client
`## Builder interview` in each `research-brief.md`. Question 1 is the one Jerel still owes: "what
does good-enough research look like before you'd let copy ship." Each question maps to a concrete
gate knob (`min_verbatim_phrases`, `required_sources`, `required_artifacts`, `thin_data_fallback`,
`compliance_constraints`, override hardness, vault-freshness). Closing section routes each answer to
either the template default, the per-client brief, or a future code change.

Writing rules applied (`~/.claude/commands/writing/SKILL.md`): 0 AI-tell words, 0 negative
parallelism, 0 curly quotes / arrows, em-dashes trimmed 23 -> 20 across 1444 words (4 swapped for
periods/colons where the pause didn't earn the dash).

## Tests (real output)

### Gate PASSES on _smoketest (exit 0)
```
RESEARCH GATE
client: _smoketest   niche: SG buyer-side flat-fee property advisory
  [PASS] source type: voice_of_customer   <- .../voc-reddit-dump-260611.md
  [PASS] source type: competitor_intel    <- .../competitor-notes-260611.md
  [PASS] source type: market_context      <- .../market-stats-260611.md
  [PASS] source type: client_assets       <- .../onboarding-form-260611.md
  [PASS] verbatim phrases: 120/20
  [PASS] artifact: icp_equivalent
  [PASS] artifact: competitor_doc
  [PASS] artifact: market_doc
  [PASS] artifact: gap_analysis           <- .../awareness-sophistication.md
  [PASS] compliance named: claims_have_sources
  [PASS] compliance named: platform_policy
OVERALL: PASS
EXIT=0
```

### Gate FAILS on the thin fixture with actionable scorecard (exit 1)
```
client: thin-fixture   niche: thinfixture widget demo
  [PASS] source type: voice_of_customer   <- .../voc-thin-260611.md
  [FAIL] source type: competitor_intel
         fix: supply a file whose name matches one of ['competitor','competitive-landscape','swipe'] ...
  [FAIL] source type: market_context
         fix: supply a file whose name matches one of ['market-stats','trend'] ...
  [FAIL] source type: client_assets
         fix: supply a file whose name matches one of ['offer','onboarding','landing'] ...
  [FAIL] verbatim phrases: 2/20
         fix: add quoted customer phrases (>=4 words each) ... you have 2
  [FAIL] artifact: competitor_doc   fix: create one of: 00_inputs/research/competitor-*.md | ...
  [FAIL] artifact: market_doc       fix: create one of: 00_inputs/research/market-*.md
  [FAIL] artifact: gap_analysis     fix: create one of: 00_inputs/research/*gap*.md
OVERALL: FAIL
EXIT=1
(+ thin-data fallback ladder + research_gate_override escape hatch printed)
```

### --json parses (machine path)
```
smoketest:    PARSED OK -> overall: PASS | items: 11 | niche: SG buyer-side flat-fee property advisory
thin fixture: PARSED OK -> overall: FAIL | fails: 7
top-level keys: client, client_root, niche, used_default_floor, vault_dossiers_matched, overall, items, thin_data_fallback, human_read_through
item keys:      category, id, label, pass, evidence, fix
```

### Ferres-floor fallback (no research-brief.md) — SAYS SO
Tested on a temp client dir with no brief (created, tested, removed — left no trace):
```
client: no-brief-probe   niche: (unknown — no research-brief.md)
NOTE: no _brand/research-brief.md found — using the Ferres-floor defaults
      (3 named artifacts, >=20 verbatim phrases, gap analysis). Add a research-brief.md to tune ...
```

## Acceptance status

| Criterion | Status | Evidence |
|---|---|---|
| Gate PASSES on _smoketest (real output) | PASS | exit 0, 11/11 items PASS (above) |
| FAILS on thin fixture with actionable scorecard | PASS | exit 1, 7 FAIL items each with a `fix:` line |
| `--json` parses | PASS | both clients parse via `json.load`; stable key set |
| avatar-research carries the canonical block verbatim | PASS | `skills/avatar-research/SKILL.md:59-61`, diff matches the task text exactly |
| Template = niche-parameterized "research complete" (YAML + prose + builder interview) | PASS (pre-existing) | `clients/_template/_brand/research-brief.md` |
| Defaults = Ferres floor | PASS | named artifacts, >=20 verbatim, gap analysis, human read-through, re-run-if-thin |
| Calibration interview written (8-12 Qs) | PASS | 12 questions, `_handoffs/research-calibration-interview.md` |
| HARD RULES honored | PASS | no network/Meta/sheet/render; live clients untouched (git status clean under eugene/neeza/takekine); _template + _smoketest only writes |

## Noticed but NOT fixed (logged, out of surgical scope)

1. **Verbatim-phrase counter over-counts on _smoketest (120 vs a real ~27).** The `RE_QUOTED_SPAN`
   heuristic in `research_gate.py:411` counts any quoted span >=4 words in a research file. On
   _smoketest it pulls competitor ad slogans out of `competitor-notes-260611.md` ("Your dream home
   is closer than you think.", "DM me 'READY' to start your journey.") and counts them as customer
   VOC. The gate still passes honestly — the real `voc-reddit-dump` alone clears 20 — but the 120
   number is inflated by advertiser copy, not buyer language. This is exactly Question 5 in the
   calibration interview (restrict the count to designated VOC files only). Recommend closing it as
   a gate change once the operator answers, not silently here.

2. **Heavy git-status noise from concurrent sibling M2 agents.** This run only touched
   `skills/avatar-research/SKILL.md` (+ the new handoff file). Other modified files in `git status`
   (`.claude/skill-graph.json`, `*-attribution.md`, `skills/ad-concept-engine/SKILL.md`,
   `scripts/hook_gate.py`, `clients/_smoketest/campaigns/feedback/wave-1/`, `M2.2`/`M2.6` reports,
   `big-angle-spotter/SKILL.md.bak-260611-m2`) are from parallel M2 tasks running in the same repo,
   not from this task. Flagging so the orchestrator doesn't attribute them to M2.1. The second hunk
   in my avatar-research diff (`[[persona-builder]]` -> `[[campaign-runner]]` graph link at line 576)
   is an auto-regeneration by `scripts/link-skills.py` triggered by a sibling, not a hand edit.

3. **No CLI test harness wires the gate into avatar-research's actual run.** The precondition is a
   documented instruction (operator/agent runs `research_gate.py` by hand). There is no PreToolUse
   hook or in-skill code path that auto-fires the gate before generation. That matches the task
   scope (precondition text only), but if the goal is hard enforcement rather than a documented
   step, a hook is the next move — out of scope here.
