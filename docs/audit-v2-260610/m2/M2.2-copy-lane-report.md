# M2.2 — Copy Lane Upgrade (headline-bank + hook gate) — Report

**Task:** M2.2-copy-lane
**Branch:** rebuild-v2
**Date:** 2026-06-11 (SG)
**Owned files:** `skills/headline-bank/SKILL.md`, `scripts/hook_gate.py`

## Starting state (important)

Almost all of this task's deliverables were already present, committed at `0fff0f4` ("chore(rebuild): m2 partial wip — stopped mid-run on operator request"). That commit added the insight-tag rule, VOC injection slots, hook gate documentation, script-skill fold-in, the research-completeness precondition, `scripts/hook_gate.py`, `scripts/research_gate.py`, and the three hook-gate fixtures.

My job this run was therefore **verification + repair of the gaps**, not greenfield authoring. I verified every acceptance criterion against real output and fixed the one genuine defect I found.

## Changes (file:line)

### 1. `scripts/hook_gate.py:222-225` — `--help` now defines the input shape (the only real fix)

The task requires the JSON shape to be defined **in `--help`**. The committed script claimed this ("see --help for shape") but argparse only printed the one-line parser `description` — the module docstring containing the full `INPUT SHAPE` / `INSIGHT-TAG FORMAT` / `PASS RULE` blocks was never surfaced by `--help`. The self-reference was circular.

```python
# before
    p = argparse.ArgumentParser(
        description="Score + insight-tag gate for ad hooks/headlines (see --help for shape).")
# after
    p = argparse.ArgumentParser(
        description=__doc__,
        formatter_class=argparse.RawDescriptionHelpFormatter)
```

`python3 scripts/hook_gate.py --help` now prints the full input-shape spec (verified below). Stdlib-only, no behavior change to the gate logic.

## Verification (real output)

### Acceptance A — length-mapping contradiction gone (quote both corrected passages)

The baseline (`5e867e5`) had `HEADLINE 1` mapped to a **~150w PRIMARY** in the core prompt but to a **~50w** copy in the output template — flipped. The fix adopted the core-prompt reading and made the template match. Current file:

- Core-prompt output format — `skills/headline-bank/SKILL.md:199-211`:
  - `## COPY A — PRIMARY (~150 words)` → `**HEADLINE 1:**`
  - `## COPY B — COMPRESSION (~50 words)` → `**HEADLINE 2:**`
- Output template — `skills/headline-bank/SKILL.md:304-316`:
  - `## COPY 1 — PRIMARY (~150 words)` → `**HEADLINE 1:**`
  - `## COPY 2 — COMPRESSION (~50 words)` → `**HEADLINE 2:**`

Both now agree: HEADLINE 1 ↔ 150w PRIMARY, HEADLINE 2 ↔ 50w COMPRESSION. Sheet Mapping rows (330-331) and the `## When to Use` / description blurb are consistent. **PASS.**

### Acceptance B — hook_gate passes good fixture, rejects (i) low score, (ii) dangling insight path

```
########## GOOD FIXTURE ##########
HOOK GATE — PASS  (3 hooks, all clear threshold 4.00 and resolve their insight tag)
file: clients/_smoketest/copy/hook-gate-fixtures/good.json
[exit 0]

########## BAD-LOWSCORE FIXTURE ##########
HOOK GATE — FAIL  (1 of 1 hooks blocked)
  HOOK #0: "Unlock your dream home today with our breakthrough advisory."
    - avg score 2.40 below threshold 4.00
[exit 1]

########## BAD-DANGLING-PATH FIXTURE ##########
HOOK GATE — FAIL  (2 of 2 hooks blocked)
  HOOK #0: "We overpaid 40-50k on our first flat. Not doing it twice."
    - insight file not found: clients/_smoketest/00_inputs/research/does-not-exist-260611.md
  HOOK #1: "28 saved listings. Zero decisions. Sound familiar?"
    - insight anchor does not resolve: #L9000 in clients/_smoketest/00_inputs/research/voc-reddit-dump-260611.md
[exit 1]
```

The dangling-path fixture exercises **both** dangling sub-cases: a missing file AND an out-of-range line anchor (`#L9000`). **PASS.**

### Acceptance C — precondition block verbatim

`skills/headline-bank/SKILL.md:88-90`, inside the inputs section, matches the required text exactly:

```
## Research-completeness precondition (rebuild M2.1)
Before generating for any client, run: python3 scripts/research_gate.py --client clients/<slug>
PASS is required. On FAIL: stop and surface the scorecard to the operator — never generate from thin research. Override only via an operator-recorded research_gate_override (with reason) in the campaign's pipeline-state.json.
```

**PASS.**

### Supporting checks

- **`--help` now shows shape:** `python3 scripts/hook_gate.py --help` prints `INPUT SHAPE`, `INSIGHT-TAG FORMAT`, `PASS RULE` blocks. **PASS.**
- **Worked-example insight tags resolve on disk:** `voc-reddit-dump-260611.md#L30` (= "We overpaid... 40-50k", verified line 30), `#L38` (= "28 saved listings", verified line 38), `buyer-profile.md#THE CORE PROBLEM` (heading present line 14). The good fixture reuses the same three tags — all resolve. The gate is honest, not a rigged pass.
- **VOC injection slots present** (`SKILL.md:139-146`): problem/agitate beat = exact customer language with a `voc:` source pointer; top objection answered in-line with proof + Claim Gate. Matches Ferres `03-angles-hooks-copy.md` §Copy rules #3/#5/#6.
- **Insight-tag rule present** (`SKILL.md:113-137`): every hook/headline must carry `insight: <file>#<line-or-anchor> — <≤8w>`; untagged = invalid. Grounded in Ferres §Hook rules #4 (hook's two jobs) and §Copy rules #3.
- **script-skill fold-in by reference** (`SKILL.md:268-280`): all four referenced paths resolve — `~/.claude/skills/script-skill/references/viral-hooks-library.md`, `references/hook-bank.md`, `cases/case_log.md`, and the skill root. Read-only, with a documented operator extension point. **PASS.**
- **No network / sheet / Meta / render calls** in `hook_gate.py`: stdlib only (`argparse, json, os, re, sys`). Read-only. **PASS.**
- **Example copy clears anti-AI rules:** worked-example hooks (133-135) are verbatim-VOC, no banned words, feed-native. The `breakthrough`/`unlock your dream home` strings appear only in the bad-lowscore fixture as an intentional negative test and in the skill's own kill-list.

### M1 preservation (must-not-weaken)

- `skills/ad-concept-engine/SKILL.md:618` — "Claim Gate (machine precondition — runs BEFORE HITL Gate 3)" intact, untouched this run.
- `skills/avatar-research/SKILL.md:410` — "Quote Provenance (HARD RULE — no source, no quote)" intact.

## Things noticed but NOT fixed (logged per instructions)

1. **`skills/avatar-research/SKILL.md` shows as modified (`M`) in `git status`** — a 6-line change (5 insertions, 1 deletion). This predates my session (it was already in the opening gitStatus snapshot's uncommitted set, part of the M0/M1 baseline). I did not touch the file; the Quote Provenance HARD RULE is fully intact. Flagging for whoever reconciles the uncommitted M1 working tree before the next commit.
2. **Good fixture hook #2** ("The 4,500 fee that stops you overpaying 50k.") tags `#THE CORE PROBLEM — advice never feels on their side`. The gate only checks tag *resolution*, not semantic fit (by design — semantic fit is the operator's 1-5 score job). The tag resolves; the semantic tie is loose but acceptable. Not a defect.
3. **`hook_gate.py` text-anchor matching is forgiving** — a bare anchor string also matches any verbatim substring anywhere in the file, not just headings (documented in the docstring). This is intentional (mirrors how operators cite mid-file phrases) but means an anchor like `#the` would resolve trivially. Low risk given operators write meaningful anchors; noting for completeness.

## Files changed this run

- `scripts/hook_gate.py` (1 edit: argparse `--help` now emits the full input-shape docstring)
- `docs/audit-v2-260610/m2/M2.2-copy-lane-report.md` (this report)

Everything else required by the task was already committed at `0fff0f4` and verified above. No live-client folders touched; no staged-handoff needed (no live-client deliverable in this task). big-angle-spotter symlink untouched (no edit needed).
