# M2.3+4 — Statics Lane Rebuild (gap rows 7, 8, 10)

**Task:** `M2.3+4-statics-lane` · branch `rebuild-v2` · 2026-06-11
**Scope:** meta-copy pre-launch gate + image-prompt method + SG locale relocation.

## Starting state (important context)

A prior session (WIP commit `0fff0f4` "m2 partial wip — stopped mid-run on operator request") had already authored the deliverable FILES: `static-image-method.md`, `copy-prelaunch-rubric.md`, the archive (`high-converting-static-brief.md` + `gut-wrenching-ad-format.md` + README), the locale-rules TEMPLATE + smoketest copy, and the two staged client copies + APPLY-NOTEs. The run stopped **before wiring the SKILLs to the new files and before finishing the locale relocation**. This task completed and hardened that work.

## Changes (file:line)

### 1. ACE SKILL.md — wired the new method + new gate; removed dangling/locale refs
- `skills/ad-concept-engine/SKILL.md:465-467` — Phase 2a "Skills loaded" + MANDATORY load repointed from the archived `high-converting-static-brief.md` → `references/static-image-method.md`; dropped skill-global `sg-cultural-guidelines.md`; added `clients/<project>/_brand/locale-rules.md` IF PRESENT. Rewrote the MANDATORY paragraph to describe the four-move Ferres method.
- `skills/ad-concept-engine/SKILL.md:644-655` — NEW section "Copy Pre-Launch Rubric (fresh-context reviewer — runs AFTER the Claim Gate, BEFORE HITL Gate 3)" + the gate-sequence diagram (Claim Gate → Copy Pre-Launch Rubric → HITL Gate 3).
- `skills/ad-concept-engine/SKILL.md:657` — HITL Gate 3 precondition now requires BOTH the Claim Gate exit 0 AND the rubric verdict PASS (or recorded override).
- `skills/ad-concept-engine/SKILL.md:200` — Language Standards locale line repointed to per-client `locale-rules.md`.
- `skills/ad-concept-engine/SKILL.md:515` — Phase 2b "Skills loaded" sg-cultural-guidelines → per-client locale-rules.md.
- `skills/ad-concept-engine/SKILL.md:728-732` — References list: added `static-image-method.md` (active) + `copy-prelaunch-rubric.md`; replaced the two dangling/locale entries with an archive-citation + per-client locale pointer.
- **M1 Claim Gate body (lines 618-642) untouched** — verified by `git diff 0fff0f4` (only the surrounding MANDATORY line, the new rubric section, and the HITL Gate 3 precondition changed; the `### Claim Gate` header, `claim_gate.py` invocation, claim-extraction description, FAIL example, and three-fix rule are byte-identical).

### 2. image-generation SKILL.md — repointed renderer to the active method
- `skills/image-generation/SKILL.md:133` — "Ad-creative quality bar" callout repointed from the archived `references/gut-wrenching-ad-format.md` → `skills/ad-concept-engine/references/static-image-method.md`; stripped "Singaporean/locale casting" (locale now comes from client `locale-rules.md` IF PRESENT).

### 3. Locale relocation completed
- `git mv skills/ad-concept-engine/references/sg-cultural-guidelines.md → _archive/references-pre-ferres/sg-cultural-guidelines.md` (switchable, not deleted — the third archived file).
- `clients/_template/_brand/locale-rules.md:80-91` — NEW §6 "Cultural sensitivity (SG-specific)" added to the SG worked example, distilling the Malay-Muslim do/don't, Islamic-finance (riba → "monthly commitment"/"financing", Musharakah Mutanaqisah), sensitive periods (Ramadan/Hari Raya/National Day), property/PDPA/MAS sensitivities, and Singlish register from the relocated file. Cited the live `corrections.md:9` (260406 riba/mortgage correction) so the rule traces to a real source.
- `_archive/references-pre-ferres/README.md` — added the `sg-cultural-guidelines.md` row to the archived-files table + a reactivation note (prefer NOT to reactivate the skill-global copy; locale belongs per-client).

### 4. copy-prelaunch-rubric.md — corrected a stale note + reconciled scoring to hook_gate.py
- `copy-prelaunch-rubric.md:7` — the file wrongly claimed "scripts/hook_gate.py does not exist in the repo" (it DOES — created in the same WIP commit). Rewrote the scoring-convention note to reuse hook_gate.py's mechanics (1-5 ints, code-decides, fail-closed on missing/malformed) with one DELIBERATE, documented divergence: a per-dimension floor of 4 instead of hook_gate.py's average, because the six dimensions map to Ferres' independently-fatal criticisms (averaging would hide a single 2 / ship a known-broken ad). Removed all references to the BAS-internal `run_pipeline.py`/`RESONANCE_GATE`/`compute_gate_verdicts`.
- `copy-prelaunch-rubric.md:13, 117-145` — decision rule + a reference Python scorer (validation lifted from hook_gate.py's `avg_score`, threshold as `min(vals) >= 4`); added a verdict annotation to the example JSON (min 3 → FAIL) so the teaching example is internally consistent with the per-dimension floor.

## Tests (real output)

```
A1  grep -ri "CPF|HFE|Chinese/Malay/Indian" skills/ad-concept-engine skills/image-generation
    → empty, exit 1   PASS (locale fully moved)
A2  both old briefs in _archive/references-pre-ferres/   PASS (high-converting-static-brief.md, gut-wrenching-ad-format.md)
A3  M1 claim-gate section header + claim_gate.py invocation present, body unchanged   PASS
A4  static-image-method.md is active in BOTH ACE SKILL.md and image-generation SKILL.md   PASS
A5  Copy Pre-Launch Rubric wired into gate sequence after the Claim Gate   PASS
A6  locale-rules.md TEMPLATE + smoketest + 2 staged copies exist   PASS (4/4)
A7  APPLY-NOTE.md per staged client (eugene-chieng, neezanizam)   PASS (2/2)
```

Gate scripts smoke-tested (no network, no spend):

```
claim_gate.py --help                              → exit 0 (executes)
claim_gate.py --gate /tmp dct-static-smoke.json   → FAIL exit 1; caught unsourced "73%" in img-03.text_on_image_hook;
                                                    sourced "S$4,500" in img-01 to offer.md ledger.
                                                    (proves static-image-method Move 3 "claim gate runs on prompt text" is real)
hook_gate.py good.json --root clients/_smoketest          → PASS exit 0
hook_gate.py bad-lowscore.json --root clients/_smoketest  → FAIL exit 1 (avg 2.40 < 4.00)
hook_gate.py bad-dangling-path.json --root ...            → FAIL exit 1 (insight file not found)
```

Worked smoke example (static-image-method.md §"Worked smoke example"): cites **Pattern 09 — Native-Organic: Notes / Handwritten / UI-Mimic** by its exact name in the live `statics-pattern-library.md:97`, and every specific is source-pointed to resolving lines in `clients/_smoketest/_brand/buyer-profile.md` (`:41` buyer-truth, `:132` MP-03 header, `:154` beliefs, `:156` raw inner dialogue, `:176` L4 awareness — all verified present and accurate).

Live-client read-only rule: `git status` of `clients/eugene-chieng/ clients/neezanizam/ clients/takekine/` → zero changes. Their locale files are STAGED at `_handoffs/staged-m2/<client>/_brand/` with APPLY-NOTEs.

big-angle-spotter symlink: untouched (no `.bak` needed — the rubric moved toward `hook_gate.py`, away from BAS internals).

Anti-AI self-check on authored prose: 0 banned AI-tell words across the new prose (locale §6, rubric edits, README). Em-dashes in §6 (8) match the locale-rules house style (sections 1-5 use the same term—gloss separator convention, 14 of them); kept for consistency.

## Acceptance status: ALL PASS (7/7 + gate smokes)

## Observations logged (NOT fixed — surgical scope)

1. **Per-skill memory carries SG/Muslim content** — `skills/ad-concept-engine/learnings.md:39,50,59,60` and `corrections.md:9` reference SG/riba/HDB. These are institutional session-memory logs (the skill-activation rule mandates keeping + loading them), not "locale rules baked into a client-agnostic skill." They are correctly out of scope; moving them would destroy the compounding-memory loop. The §6 cultural block I added actually CITES `corrections.md:9` as a source, so the relationship is now explicit.
2. **Illustrative locale in templates** — `references/dct-tracker-template.md:4` has a campaign-name EXAMPLE (`META_CONV_Malay-Muslim-Buyers_SG_260408`) and `references/sophistication-creative-map.md:112` notes "Singlish/colloquial signals authenticity" as a general L4/L5 register note. Neither is a hard locale rule; left as-is.
3. **`nano-banana-examples.md` "ethnicity" fields** — these are generic image-prompt SCHEMA fields (a person's appearance descriptor), not SG-locale rules. Left as-is.
4. **link-skills.py not run** — I edited SKILL.md reference CONTENT but created/renamed no skill or agent; the skill-graph keys on names/descriptions, not reference filenames (grep found no graph references to the moved files). Running it would be no-op churn on unrelated entries. Flagging in case the operator wants a graph refresh anyway.
5. **Image-generation SKILL.md still says "Nano Banana 2"** in places (e.g. output schema "1:1 square 1024x1024" at ACE SKILL.md:472) — the Ferres method prefers 4:5 portrait + Nano Banana Pro / ChatGPT Image 2 routing for text-heavy statics. The new `static-image-method.md` carries the correct 4:5 guidance, but the legacy Phase 2a "Rules" block (ACE SKILL.md:472) still says 1:1. Not fixed (would touch the M1-era Phase 2a schema beyond the load-reference wiring); flagging as a follow-up for whoever owns the Phase 2a output-schema modernization.

---

## Adversarial verifier appendix (2026-06-11, independent re-check)

Verifier re-ran every acceptance criterion. Result: **PARTIAL** — 8/9 reproduce; one load-bearing dangling reference inside the claimed scope was introduced by the `git mv` of `sg-cultural-guidelines.md` and not caught by the builder's narrow grep.

**Reproduced clean (8):** M1 claim-gate body byte-identical (diff HEAD→WT empty for the §"Claim Gate" block); both old briefs archived with switchback note; new method cites all 5 formats + 11 patterns (table + library load instruction); claim_gate independently FAILs an unsourced "73%" in `text_on_image_hook` (exit 1); copy-prelaunch-rubric is 6 dims × 1-5 × floor-4, fresh reviewer, JSON contract, code-decides, reference scorer correct under unit test; hook_gate.py --help exit 0; locale-rules in all 4 locations + 2 APPLY-NOTEs; live clients (eugene/neeza/takekine) zero git changes; smoke example source pointers (`buyer-profile.md:41/132/154/156/176`) all resolve and Pattern 09 name byte-exact with `statics-pattern-library.md:97`.

**MUST-FIX (1, in-scope):** `git mv` of `sg-cultural-guidelines.md` → archive left a live load instruction dangling INSIDE ad-concept-engine. `skills/ad-concept-engine/references/headline-validation-checklist.md:58` still says `Load the relevant cultural guidelines from references/sg-cultural-guidelines.md` — and that checklist is a loaded ACE artifact (ACE SKILL.md:198 Phase-2 pass requirement + :731 References list). The file it points to no longer exists at that path. The builder's grep was scoped to literal locale terms (`CPF|HFE|Chinese/Malay/Indian`), which do not appear in a file-PATH reference, so the rename's dangling pointers slipped through (the CLAUDE.md "No Semantic Search" rename rule warns exactly this). Fix: repoint `headline-validation-checklist.md:58` to the per-client `clients/<slug>/_brand/locale-rules.md` IF PRESENT (consistent with the rest of the relocation), or to the archive path if the skill-global copy is genuinely needed there.

**OUT-OF-SCOPE observations (log only, do not fix here):**
- A) Same `sg-cultural-guidelines.md` move left 4 dangling refs in a DIFFERENT skill: `skills/source-of-truth/SKILL.md:269`, `references/section-synthesis-frameworks.md:11`, `references/triage-questions.md:84`, `references/research-pipeline.md:109`. source-of-truth is outside the two skills this task claimed (`ad-concept-engine` + `image-generation`), so it is out of this task's surgical scope — but the move broke them and a follow-up task (or the operator) must repoint them, else SG source-of-truth runs hit a missing file.
- B) BAS `SKILL.md.bak-260611-m2` exists and BAS SKILL.md differs from it, but the delta is research-gate + diversity-map content (M2.1 / M2.6 tasks), NOT statics — consistent with the builder's "BAS untouched by THIS task" claim. Not a defect of M2.3+4.
- C) `scripts/hook_gate.py` carries a 2-line diff (RawDescriptionHelpFormatter + `__doc__`) — benign, in-scope (it's what makes `--help` exit 0 with the full description the rubric reuses).
- D) Builder's "claim_gate.py --help exit 0" is correct only from REPO ROOT (`python3 scripts/claim_gate.py`); the script lives at repo-root `scripts/`, not under the skill. SKILL.md uses the repo-root-relative path, so this is correct by design, not a bug.

**Verdict: PARTIAL.** The statics-lane rebuild is substantively sound and the M1 work is preserved intact. One in-scope dangling reference (`headline-validation-checklist.md:58`) must be repointed before this passes clean.

## Fix round (2026-06-11)

Verifier MUST-FIX: `headline-validation-checklist.md:58` dangling load instruction. The checklist is a LIVE loaded ACE artifact (named at SKILL.md:198 as a Phase-2 pass requirement, and SKILL.md:731 lists it in References), but §4 "Cultural Sensitivity" still instructed agents to load `references/sg-cultural-guidelines.md` — a file git-mv'd to `_archive/references-pre-ferres/`. An agent running headline validation would hit a missing file.

### Change (file:line)
- `skills/ad-concept-engine/references/headline-validation-checklist.md:58` — repointed the §4 load instruction from the archived `references/sg-cultural-guidelines.md` to `clients/<project>/_brand/locale-rules.md` IF PRESENT (template + SG example at `clients/_template/_brand/locale-rules.md`), with the archive pointer noted. Matches the relocation phrasing already used at SKILL.md:200 and :732 verbatim. No other line touched.

### Tests (real output)
1. Edited line reads: `Load the client's locale rules from clients/<project>/_brand/locale-rules.md IF PRESENT (template + SG example at clients/_template/_brand/locale-rules.md). The old skill-global sg-cultural-guidelines.md is archived at _archive/references-pre-ferres/. Quick checks:` — PASS.
2. `grep -rn 'references/sg-cultural-guidelines.md' skills/ad-concept-engine/` → exit 1 (no dangling load path remains). PASS.
3. New pointer targets resolve on disk: `clients/_template/_brand/locale-rules.md` exists; `_archive/references-pre-ferres/sg-cultural-guidelines.md` exists. PASS.
4. Convention consistency: checklist phrasing now matches SKILL.md `locale-rules.md IF PRESENT` pattern (1 occurrence). PASS.

### Acceptance
PASS. The single in-scope dangling reference that held the original verdict at PARTIAL is resolved. No M1 work touched (Claim Gate + Quote Provenance untouched). Surgical — one line changed inside an owned file.

## Adversarial verifier — second pass (2026-06-11, post-fix-round)

Independent re-check of all 9 criteria, with focus on the previously-failed `headline-validation-checklist.md:58`. **Verdict: CONFIRMED.** Every criterion reproduces.

- Prev MUST-FIX resolved: `headline-validation-checklist.md:58` now points at per-client `clients/<project>/_brand/locale-rules.md` IF PRESENT; `grep -rn 'Load.*references/sg-cultural-guidelines' skills/ad-concept-engine skills/image-generation` → exit 1 (no dangling load path). The two remaining `sg-cultural-guidelines` mentions in ad-concept-engine (SKILL.md:732, checklist:58) are both archive-pointer prose, not load instructions.
- M1 Claim Gate prose (header → "never a skipped step.") diffed byte-identical vs 0fff0f4. avatar-research Quote Provenance HARD RULE (SKILL.md:410) + provenance gate (:481) intact.
- Gate scripts re-run: `hook_gate.py` PASS/FAIL/FAIL → exit 0/1/1 (good / avg 2.40<4.00 / dangling anchor); `claim_gate.py --gate` on the smoke dct → exit 1, caught unsourced "73%" in `text_on_image_hook` + `image_prompt` + `visual_style`. Rubric reference scorer unit-tested: all≥4 PASS, single-3 FAIL, bool/out-of-range fail-closed.
- New method cites all 5 formats + 11 patterns (library has exactly 11; Pattern 09 name byte-exact with `statics-pattern-library.md:97`); 5 smoke source pointers resolve in `_smoketest/buyer-profile.md`.
- Locale relocation: 4 locations + 2 APPLY-NOTEs present; live clients (eugene/neeza/takekine) zero git changes. §6 cultural block cites live `corrections.md` 260406.
- BAS untouched by this task (SKILL.md delta vs `.bak-260611-m2` is M2.1 research-gate + M2.6 diversity-map only, zero statics/locale refs). Out-of-scope source-of-truth dangling refs (4) correctly logged, not fixed.
