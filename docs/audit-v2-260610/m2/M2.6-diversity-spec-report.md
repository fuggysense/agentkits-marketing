# M2.6 — Angle Volume/Diversity Spec for big-angle-spotter

**Task ID:** M2.6-diversity-spec
**Date:** 260611
**Branch:** rebuild-v2
**Owned files:** `skills/big-angle-spotter/SKILL.md` (symlink → `~/AI workflows/big-angle-spotter/SKILL.md`)

## Summary

Added the angle volume/diversity spec to big-angle-spotter's SKILL.md (the gap row 5 remainder):
a REQUIRED hardened-mode diversity-map input, a coverage rule (>=6 distinct map entries before the
gate banks winners), the 1%-remix warning, and the verbatim research-completeness precondition
block. Wrote a worked diversity-map example for `_smoketest` grounded entirely in its real research
files.

All changes are docs/spec only. `run_pipeline.py` NOT touched (M1.6 owns it). No network/sheet/Meta
writes, no render spend. Live clients untouched.

## Changes (file:line)

### `~/AI workflows/big-angle-spotter/SKILL.md` (symlink target — `.bak-260611-m2` created first)

1. **Line 51–53 — verbatim research-completeness precondition block.** Added EXACTLY the block
   specified (heading `## Research-completeness precondition (rebuild M2.1)`, the
   `python3 scripts/research_gate.py --client clients/<slug>` run line, PASS-required, FAIL =
   stop + surface scorecard, override only via `research_gate_override` in pipeline-state.json).
   Placed in the inputs section, right after step 2 (Write `inputs.json`), before step 3.

2. **Line 74 — diversity-map cross-reference** in step 4's hardened-inputs list, so the launch
   point and the Hardened-mode section don't contradict.

3. **Line 178 — diversity map as 4th REQUIRED hardened input.** 20–30 mind-mapped reasons to buy,
   each tagged awareness + emotion, sourced from buyer-profile + research, living at
   `clients/<slug>/angles/diversity-map-<date>.md`.

4. **Lines 180–194 — new subsection "Diversity map — shape, coverage rule, and the 1%-remix
   warning":**
   - **Map shape:** plain md list/table; each entry = reason + awareness level + emotional driver +
     source line. 20–30 entries spread across levels/emotions. (Grounded in Ferres
     `05-quality-bar-critique-rubric.md` §5 piano metaphor + `03-angles-hooks-copy.md` Angle
     taxonomy 3.)
   - **Coverage rule (pre-gate checklist):** candidate angles must collectively cover >=6 distinct
     map entries before banking — documented as an INPUT spec the orchestrator checks, explicitly
     NOT a code change to the gate (the gate scores quality; diversity is upstream).
   - **1%-remix warning:** near-duplicate angles count as ONE idea (Meta similarity score /
     Andromeda); flag any cluster where >2 candidates map to the same entry, collapse to one,
     backfill from uncovered entries. (Grounded in `05` §5 + `03` Iteration 4.)

### `clients/_smoketest/angles/diversity-map-260611.md` (NEW)

Worked 24-entry diversity map for the fictional Meridian Property Advisory smoke client. Spans all
five Schwartz awareness levels (Unaware → Most-aware) and nine distinct emotional drivers. Every
entry cites a real source line. Includes a coverage read, a 1%-remix watch, and a tie-back to the
existing `angles/run-260611/` run (which already flagged A07/A09 as the exact remix problem this
map prevents upstream).

## Tests (real output)

**1. `.bak` exists at symlink target:**
```
-rw-r--r-- 18756 11 Jun 01:27 SKILL.md.bak-260611-m2
```

**2. Verbatim block exact-match (grep):**
```
51:## Research-completeness precondition (rebuild M2.1)
52:Before generating for any client, run: python3 scripts/research_gate.py --client clients/<slug>
53:PASS is required. On FAIL: stop and surface the scorecard to the operator — never generate from thin research. Override only via an operator-recorded research_gate_override (with reason) in the campaign's pipeline-state.json.
```
Matches the spec character-for-character.

**3. Map shape + coverage rule + remix warning present (grep):** lines 178 (input), 180 (subsection
heading), 192 (coverage rule, "at least 6 distinct map entries"), 194 (1%-remix warning, "more than
two candidates map to the same diversity-map entry"). All present.

**4. Smoke example resolves to real research lines.** Every cited VoC quote number (q2,3,4,5,6,7,9,
10,11,12,14,16,17,18,19,20,21,23,24,25,26,27) resolves to a real line in
`voc-reddit-dump-260611.md` with the framing quoted. Verified via grep — sample:
```
q3 -> "Sold our flat in 3 weeks... same agent is much less helpful lol. Makes sense, less money for them on the buy."
q9 -> "I've had the PropertyGuru app open for like 4 months. I have 28 saved listings. Zero decisions."
q26 -> "...Fix the incentive and I'm in."
```
Market stats 3, 4, 5, 6 resolve in `market-stats-260611.md`. Competitor §1/§2/§3 (SkylineEdge,
PropData Pro, The Honest Agent SG) resolve in `competitor-notes-260611.md`. Buyer-profile "BIGGEST
FEARS" #1, buyer-truth line, and raw inner dialogue all resolve.

**5. M1 / M1.6 preservation:**
- ad-concept-engine "Claim Gate" anchor: present in `skills/ad-concept-engine/SKILL.md` (untouched).
- avatar-research "Quote Provenance"/provenance anchor: present in `skills/avatar-research/SKILL.md`
  (untouched).
- big-angle-spotter SKILL.md hardened-mode / 10-5-5 / scored-gate content: 23 grep hits, intact.
- `run_pipeline.py` mtime 11 Jun 01:07 — unchanged (M1.6's build time; I never edited code).
- My only repo-tracked footprint: `?? clients/_smoketest/angles/diversity-map-260611.md`. The
  SKILL.md edit lives at the symlink target outside the repo, so it correctly does not appear in
  `git status`.

## Acceptance

| Criterion | Status | Evidence |
|-----------|--------|----------|
| `.bak` exists | PASS | `SKILL.md.bak-260611-m2` at symlink target, created before edits |
| SKILL.md documents map shape | PASS | line 178 (input) + 180–190 (shape: reason + awareness + emotion + source) |
| SKILL.md documents coverage rule | PASS | line 192 — >=6 distinct map entries, framed as orchestrator pre-gate check (not a code change) |
| SKILL.md documents remix warning | PASS | line 194 — >2 candidates on one entry = flag + collapse + backfill |
| Verbatim research-gate block added EXACTLY | PASS | lines 51–53, character-for-character match |
| Smoke example resolves to real research lines | PASS | all 22 VoC quotes + 4 market stats + 3 competitor sections + buyer-profile fears verified via grep |

## Notes / things I noticed (logged, not fixed — per instructions)

1. **`scripts/research_gate.py` does not exist yet.** The verbatim block I was told to add points at
   `python3 scripts/research_gate.py`, but no such script is in `skills/big-angle-spotter/scripts/`
   or anywhere in the repo / symlink target. This is a forward reference — the block is tagged
   "rebuild M2.1", so a sibling M2.1 task presumably owns the script. The SKILL.md instruction is
   correct as written; the script is the dependency. Flagging so M2.1 closure is tracked.

2. **Pre-existing staged deletion unrelated to this task:**
   `D skills/ad-concept-engine/references/high-converting-static-brief.md` was already staged in the
   working tree before I started. I did not touch ad-concept-engine. Logging only.

3. **The `_template/` analogue.** The diversity-map example was written for `_smoketest` (per spec).
   No template stub was added to `clients/_template/angles/` — the task scoped the worked example to
   `_smoketest`. If a blank `diversity-map-template.md` is wanted in `_template/` for new-client
   scaffolding, that's a follow-up, not in this task's acceptance.
