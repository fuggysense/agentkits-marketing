# M2.7 — Feedback-Router Repair + Media-Buying Doctrine

**Task ID:** M2.7-feedback-repair
**Date:** 260611
**Branch:** rebuild-v2
**Owned files:** `skills/feedback-router/SKILL.md` + `references/`

## Summary

The router repair (SKILL.md, routing-criteria.md, the new media-buying-doctrine.md, learnings-template.md) was already written in the prior M2 WIP commit `0fff0f4` ("M2.7 partially written; verify + smoke phases never ran"). This pass did the unfinished half: verified every acceptance criterion against the committed files, built a post-launch walkthrough fixture with real fictional performance numbers in the writable `_smoketest` client, added a `feedback_thresholds` block to its metrics-config, and ran the router end to end. The walkthrough lands on **BETTER** citing only live entries.

No content rewrite of the owned files was needed — they pass as committed. My net session changes are two fixtures + this report. No live-client file touched. No network/sheet/Meta call. No render spend.

## Verification of the committed repair (gap rows 11+12)

Each repair item from the brief, with the grep/read that confirms it:

### (a) Input = current `dct.json` (image_pool) + sheet-writer conventions; legacy documented as fallback
- SKILL.md L51–54 reads the wave `dct.json` (`dcts[].image_pool`) as the current spine, the CREATIVES/COPY tabs (keyed by `BATCH`, written by `ad_concept_sheet_writer.py`, metric columns filled by `sheets-updater`/`meta_puller`) as the synced source, and `dct-tracker.json` (`creatives[]`, `batch` key) explicitly as legacy fallback only.
- Both shapes exist as fixtures and both parse: current `dcts[].image_pool` and legacy `creatives[].batch` (see Tests).

### (b) All meta-ads-MCP references → the meta CLI (read verbs only)
- SKILL.md L29, L55, L99, L198, L225 all route live numbers through `~/.local/bin/meta` read verbs (`meta ads insights get`, billing-safe), and state "No meta-ads MCP exists" / "there is NO meta-ads MCP." The auth pattern (`source ~/.claude/.env && export ACCESS_TOKEN="$META_ACCESS_TOKEN"`, `meta auth status`, `-o json`, scope by `ad_account_id`) matches the canonical `.claude/rules/mcp-integrations.md` L29 verbatim.
- Acceptance grep: 0 lines prescribe an MCP as the data source.

### (c) Dead `/ads:concepts` + `/ads:avatars` routes → live intent-routed entries
- Every `/ads:*` mention in the owned files is annotated "are dead" / "intent-routed." NEW routes to the `source-of-truth` + `avatar-research` skills; BETTER/MORE route to `ad-concept-engine` Conductor Mode. All four target skills exist on disk, and the routing-overrides.md "DCT conductor" (L16) + "build avatar → avatar-research" (L17) entries the router cites are present.
- Acceptance grep: 0 `/ads:*` lines presented as a live route.

### (d) Thresholds → per-client config (currency follows config, not hardcoded S$)
- SKILL.md L88 + routing-criteria.md §Threshold tuning resolve in order: `feedback_thresholds{}` → legacy `feedback_router_thresholds{}` → routing-criteria.md defaults. Currency reads from `feedback_thresholds.currency` / ad-account currency, never a hardcoded `S$`. The `tcpr × tcpr_kill_multiple` pair feeds the doctrine's 3x-TCPR kill gate.
- Walkthrough confirms: thresholds resolved from `feedback_thresholds (client metrics-config)`, currency `SGD` from config, kill gate computed as `SGD 300`.

### S-06 fail-closed (the strength being preserved, not weakened)
- Pre-routing gates in routing-criteria.md L11–23 still emit `INSUFFICIENT_DATA → Do not route → surface the gap`, mirrored in SKILL.md Phase 0 L90 ("surface the gap, stop. Don't route on thin data"). The pre-launch fixture (zero metrics) correctly trips this path (see Tests).

### Doctrine file (gap row 12) — added from Ferres with ⚠️PLATFORM tags
`references/media-buying-doctrine.md` exists (129 lines, 4 PLATFORM tags) and carries every Ferres element the brief named, each cited to `07-media-buying-testing-scaling.md`:
- §1 TCPR + 3x-before-kill rule (kill/keep examples, early-kill + early-stop exceptions)
- §2 vertical (20–30%/day) + horizontal (50–100% duplicate) scaling; 25–35-unique-ad ceiling ⚠️PLATFORM; 30-day LTV:CAC ≥ 3:1 gate
- §3 fatigue 3-condition trigger (freq >3 AND 7d CPA 30%+ worse AND thumbstop/CTR/watch down) ⚠️PLATFORM; statics-fatigue-faster
- §4 80/20 next-batch mix (the MORE/BETTER engine) + the $100/5-statics cheap pre-test ⚠️DATED
- §5 comment-mining as a standing objection-research input feeding the next batch
- §6 TAM-exhaustion diagnosis (angle pool scooped, not copy broken) on the BETTER route, with the symptom-to-route map

The SKILL.md cites the doctrine at Phase 2 Pre-step A/B (kill + comment-mining) and in both BETTER (TAM) and MORE (80/20) actions.

## Changes (file:line) — this session only

1. `clients/_smoketest/_brand/metrics-config.json` (+14) — added a `feedback_thresholds{}` block (SGD, tcpr 100, kill-multiple 3, the spend/impression/ratio/frequency floors) so the walkthrough exercises the per-client config path instead of the file defaults. Smoke client only.
2. `clients/_smoketest/campaigns/feedback/wave-1/dct.json` (new) — post-launch wave fixture with fictional blended ad-set metrics + per-image asset CTR, grounded in the smoke client's real A01 (MP-01 incentive-flip) and A02 (MP-03 free-isn't-free) angles. Shaped to land on BETTER and to plant a TAM-exhaustion sibling (A03) and a recurring un-answered objection (A02). Filled the empty `campaigns/feedback/.gitkeep` placeholder the WIP had staged.
3. `docs/audit-v2-260610/m2/M2.7-feedback-repair-report.md` (new) — this report.

Owned SKILL.md + references/ unchanged this session (verified identical to WIP commit `0fff0f4`, except an auto-generated skill-graph edge weight `0.13→0.12` flipped by a parallel `link-skills.py` run — left alone, it sits in the `do not edit by hand` block).

## Tests (real output)

### Test 1 — Full router walkthrough on the post-launch fixture → BETTER
Ran the SKILL.md phases by hand against `feedback/wave-1/dct.json` + `_smoketest` config:

```
[Phase0] thresholds src=feedback_thresholds (client metrics-config) | currency=SGD | TCPR SGD 100 3x-gate SGD 300 | KPI SGD 300
[Phase0] pre-routing gates pass = True (dur 13d, batch spend SGD 1940)
[Phase2 BETTER] winner-clear C1=True (SGD 247<=KPI & <=0.7x next-best SGD 370=259)
[Phase2 BETTER] intra-angle CTR variance C2=True (72%) | headroom C4=True (freq 1.8<2.5)
[Phase2 doctrine §6] A03 TAM-exhaustion=True (CPA SGD 210->SGD 430, freq 2.1 moderate) NOT fatigue
>>> ROUTE = BETTER
>>> winning angle A01 The incentive flip; failure pattern: UGC-documentary 3.9% beats Editorial-data 1.1% (3.5x)
>>> NEXT INTENT (live, intent-routed): ad-concept-engine Conductor Mode — 'refine within A01: The incentive flip' + widen A03 (TAM) + 80/20 mix; all output currency=SGD
```

The decision cites only live entries: `ad-concept-engine` Conductor Mode (intent-routed per routing-overrides.md). Currency is SGD from config throughout. The 3x-TCPR kill gate (SGD 300) ran on every DCT; A02 (CPA SGD 520, cleared the gate) is the eligible cut; A01 + A03 keep. TAM-exhaustion on A03 is named, not misread as fatigue (frequency 2.1 never breaches 3).

> Honest note on fixture tuning: the first draft of A03 had CPA SGD 280, only 12% above A01's SGD 247, so the `winning_angle_cpa_ratio_to_next_best: 0.7` gate (needs A01 ≤ 259) returned False and no route fired cleanly. That is the threshold working, not a bug. I widened A03's blended CPA to SGD 370 (its week2 climb to SGD 430 pulling the blend up) so A01 reads as a decisive winner and BETTER fires on its own conditions rather than the precedence tiebreak.

### Test 2 — S-06 fail-closed path (pre-launch fixture, zero metrics)
```
[S-06 fail-closed test] pre-launch wave-smoke-260611: any DCT has metrics? False
  => router output: INSUFFICIENT_DATA (STOP, surface gap) -- fail-closed mechanism intact
```

### Test 3 — Legacy `dct-tracker.json` fallback recognised
```
[legacy fallback test] dct-tracker.json top keys: ['dct_id', 'creatives']
  has creatives[] (not dcts[])? True
  batch key on creative[0]: 'DCT010-A01' -> maps to DCT/angle key per SKILL.md fallback rule
```

### Test 4 — Acceptance grep sweep (owned files)
```
-- dead commands as LIVE routes (must be 0): 0
-- MCP as prescribed data source (must be 0): 0
-- PLATFORM tags in doctrine (must be >=1): 4
-- doctrine file exists: YES
```

### Test 5 — JSON validity
Both `feedback/wave-1/dct.json` and `_smoketest/_brand/metrics-config.json` parse clean.

## Acceptance status

| Criterion | Status | Evidence |
|---|---|---|
| Greps clean (no dead commands / MCP) | PASS | Test 4: 0 / 0 |
| Doctrine file exists with PLATFORM tags | PASS | Test 4: file present, 4 PLATFORM tags |
| Router walkthrough reaches a routing decision citing live entries | PASS | Test 1: BETTER → `ad-concept-engine` Conductor Mode (intent-routed) |
| S-06 fail-closed preserved (plumbing repaired, not weakened) | PASS | Test 2: INSUFFICIENT_DATA on thin data |
| Per-client thresholds + currency from config | PASS | Test 1: src=`feedback_thresholds`, currency SGD from config |
| Legacy `dct-tracker.json` documented + recognised | PASS | Test 3 + SKILL.md L54/L96 |
| M1 work preserved | PASS | ad-concept-engine Claim Gate (2 hits), avatar-research provenance (5 hits) intact |
| Live clients READ-ONLY | PASS | `git status` shows no eugene-chieng / neezanizam / takekine file modified |

## Noticed, not fixed (logged per "surgical changes only")

1. **Auto-graph churn on an owned file.** `skills/feedback-router/SKILL.md` shows a 1-line working-tree diff vs HEAD (`[[sheets-updater]] (skill, 0.13)` → `0.12`) from a parallel `link-skills.py` run. It is inside the auto-generated `skill-graph` block. Left untouched.
2. **`_smoketest` metrics-config is the unfilled template copy** (placeholder gids `{{GID_*}}`, slug `{{CLIENT_SLUG}}`). The walkthrough only needs the `feedback_thresholds` block I added, so this did not block the test, but a future smoke pass that exercises the sheet-tab read (CREATIVES/COPY gids) would need a filled copy. Not in scope here.
3. **Shared working tree is noisy.** Many unrelated files (M2.1, M2.2, ad-concept-engine, avatar-research, scripts) are dirty from parallel M2 agents. I scoped all my reporting to my owned + fixture paths only.

## Unresolved questions

- None blocking. If the operator wants the router to also walk a NEW or MORE outcome on a fixture (not just BETTER), I can add two sibling wave fixtures — the threshold logic for all three is already verified branch-by-branch in routing-criteria.md, but only BETTER was driven end to end here.
