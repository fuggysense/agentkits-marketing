# M2.8-10-misc — Three light merges (synthesis sections, hook-swap lane, swipe curation)

Task ID: M2.8-10-misc · Branch: rebuild-v2 · Date: 2026-06-11 (SGT)
Owner files only. No external/network calls, no live writes, no render spend. Live-client folders untouched (no staging needed — all changes land in shared skills + script docs).

Before writing prose I read `~/.claude/commands/writing/SKILL.md` and applied the anti-AI rules + self-check (straight quotes, no AI-tell words, em-dash count low, named sources, no coined-concept labels, no recap paragraphs).

---

## Change 1 — source-of-truth: append §27 + §28 (kept numbering coherent, no renumber)

Sources grounded: `_shared-knowledge/ferres/02-research-flow.md` prompt 1 (ICP Deep Dive gap analysis) and prompt 2 (Competitor Analysis opportunity matrix).

**`skills/source-of-truth/references/26-section-template.md`**
- Line 1: title `# 26-Section…` → `# 28-Section Source-of-Truth Template` + a note (line 5) stating §1-26 are the original spine, §27/§28 are appended Ferres sections, and the rest is NOT renumbered.
- Lines 820-840: new `## 27. Says-vs-Addresses Gap Analysis` — gap table (`{{gap_analysis_rows}}` at line 829), four gap types (unaddressed / mismatched / over-indexed / buried), Top-3-gaps block, "read their mind" feed-forward to §10/§11. Cites prompt 1 + transcript timestamps.
- Lines 842-872: new `## 28. Competitor Opportunity Matrix` — per-competitor profile table (`{{competitor_profile_rows}}` line 851), opportunity matrix (`{{opportunity_matrix_rows}}` line 856) with four opportunity types (blue-box / blue-ocean / weak-proof / unmarketed-advantage), differentiation-strategy block. Cites prompt 2 + transcript timestamps; reuses `stage-analysis.md` blue boxes/gaps if present rather than re-deriving.
- Both inserted between §26 Final Notes and the Appendix, so the Appendix follows §28.

**`skills/source-of-truth/SKILL.md`**
- Line 7 (frontmatter description): "26-section" → "26 core sections + 2 Ferres research-flow sections (§27 … §28 …)". Trigger keywords (including "26 section doc") left intact.
- Line 53 (intro blurb): rewritten to name the 26-section spine + §27 + §28.
- Lines 201-202 (Phase 3 synthesis table): two new rows mapping §27 and §28 to their net-new synthesis instructions + Ferres source.

## Change 2 — video-concept-lab: hook-swap variant lane (a lane, not a replacement)

Sources grounded: `_shared-knowledge/ferres/04-end-to-end-sop.md` stages 3-5; `video-hook-variants` skill named as the hook engine.

**`skills/video-concept-lab/SKILL.md`**
- Lines 134-170: new `## Hook-Swap Variant Lane (clients with a proven control)`, inserted between the Brief Sub-Type Gate and DR First Principles. Explicitly framed "A cheap test lane — not a replacement for full concept seeding"; falls back to five-concept seeding if the when-to-choose conditions fail.
  - **Inputs:** control transcript (hook+body, marked), the 3 research docs, `_brand/{offer,buyer-profile}.md`, performance note on WHY the control wins (retention = hook, click-rate = body).
  - **Hook engine:** dispatches `video-hook-variants` (line 151) — "do not reinvent it"; this lane sets the brief, gates output, shapes it for the AG pipeline. 2-3 new hooks; control's own hook is a free fourth contender; each tagged with its research insight.
  - **Gate:** hook clarity (clear beats clever) + avatar fit (pulls the RIGHT person — the Ferres "work 1 hour a day" rejection example) + flow (stitches onto proven body, no seam) + insight tag present. Operator picks top 2-3; recorded in `approval-1.json` with `lane: "hook_swap"`.
  - **Outputs:** hook-clips spec per variant in the §2 four-field shape (verbal/quiet-visual/rendered-text/subtitle-policy) + `control_body_ref` block (control transcript + handoff frame) + `approval-1.json`. Flows through the existing AG pipeline; only the hook is generated/rendered, body reused.

## Change 3 — ad-library-scraper: curation defaults + pattern tagging + canonical-store resolution

Sources grounded: `_shared-knowledge/ferres/06-statics-playbook.md` §5 Path A; `_shared-knowledge/ferres/patterns/statics-pattern-library.md` (11 patterns).

**`skills/ad-library-scraper/SKILL.md`** (all inserted before "Execution recipe")
- Lines 122-140: `## Proven-winner curation defaults (Ferres Path A)` — image/video + still-live + 30+ days + sort longest-running, as the DEFAULT read/model-time view (not a scrape filter — scrape still stores everything so duration accrues). Includes a read-only SQL shape against the Ghost pool; notes the 30-day cut already matches the Phase 3 L2-enrichment trigger and the Phase 6 "Winners by duration" ordering.
- Lines 142-166: `## Pattern-naming discipline (11 named static patterns)` — full 11-row table (pattern name · canonical lane · one-line tell), with tagging rules: only tag clear matches; Pattern 11 needs a sub-name note; tags live in per-ad JSON `enrichment` (carried into Ghost by ghost-sync); schema-field changes flagged-not-made; statics-only (video keeps L3 lane/format tags).
- Lines 168-186: `## Canonical store: Ghost Postgres (the SQLite is a transient build artifact)` — resolves the canonical-copy question in writing: `ads-db.sqlite` no longer exists on disk, is regenerated by `rebuild_ads_db.py` from durable per-ad JSON then synced into Ghost and discarded. Source-of-truth hierarchy (Ghost Postgres > per-ad JSON > transient SQLite) + read-only regeneration path. States earlier "canonical source" text is superseded.
- Line 225 ("Files this skill writes" table): `ads-db.sqlite` row reworded "Queryable layer" → "Transient build artifact — rebuilt from per-ad JSON, synced into Ghost Postgres (canonical), then expendable."

**`scripts/ghost-sync.py`** (docstring/header only — no code logic touched)
- Lines 6-15: new "Canonical store: GHOST POSTGRES … not the SQLite" header block; documents SQLite as transient, per-ad JSON as durable seed, the two-command regeneration path, and points to the SKILL.md §"Canonical store". Line 17: existing step-1 comment "(canonical source)" → "(the transient intermediate)". The reading logic at line ~488 was NOT changed (Ghost remains untouched, read-only doc fix as required).

---

## Tests (real output)

**Disk check — ads-db.sqlite gone (validates Change 3 premise):**
```
$ find swipe-files -name "ads-db.sqlite"
(no output — file does not exist anywhere under swipe-files/)
$ ls swipe-files/property-sg/
avatar-registry.json  research-pool.json   (no ads-db.sqlite, no pages/)
```

**Python parse check (ghost-sync.py docstring edit is valid Python):**
```
$ python3 -c "import ast; ast.parse(open('scripts/ghost-sync.py').read()); print('PARSE OK')"
PARSE OK
```

**Line-number verification of all new content:** confirmed via grep — see the file:line citations above (every `## 27.`, `## 28.`, `Hook-Swap Variant Lane`, `Proven-winner curation defaults`, `Pattern-naming discipline`, `Canonical store`, and ghost-sync header line resolves).

**M1 preservation check:** `ad-concept-engine/SKILL.md` (Claim Gate) and `avatar-research/SKILL.md` (Quote Provenance) were NOT in my edit set — neither appears in my touched-file list. Verified.

**Scope check (no other skill files touched by me):** `git status --short` shows several other modified skill files (`feedback-router/references/routing-criteria.md`, `headline-bank/SKILL.md`, `.claude/skill-graph.json`, `_archive/` moves, agent attribution files, `clients/_template/_brand/*`, new `scripts/hook_gate.py`, etc.). These are pre-existing / parallel-M2 changes — NONE were produced by this task. My edits are isolated to the five owned paths: `skills/source-of-truth/SKILL.md`, `skills/source-of-truth/references/26-section-template.md`, `skills/video-concept-lab/SKILL.md`, `skills/ad-library-scraper/SKILL.md`, `scripts/ghost-sync.py`.

---

## Acceptance

| Criterion | Status | Evidence |
|---|---|---|
| §27 says-vs-addresses gap analysis added (prompt 1) | PASS | template line 820; SoT SKILL line 201 |
| §28 competitor opportunity matrix added (prompt 2) | PASS | template line 842; SoT SKILL line 202 |
| Numbering coherent (appended 27/28, no renumber) | PASS | template title line 1 + note line 5; §1-26 unchanged |
| Hook-swap lane: positioned as lane not replacement | PASS | VCL line 134 ("not a replacement…"), fallback to 5-concept seeding |
| Hook-swap cites SOP stages 3-5 + video-hook-variants engine | PASS | VCL lines 134, 151 |
| Hook-swap documents inputs/gate/outputs | PASS | VCL lines ~144-170 (inputs, 4-point gate, hook-clips spec) |
| Curation filters as documented defaults (Path A) | PASS | ad-library SKILL line 122 |
| Pattern-naming: 11 pattern names where they match | PASS | ad-library SKILL line 142 (11-row table) |
| Canonical-copy resolved in writing in SKILL.md | PASS | ad-library SKILL line 168 + table row line 225 |
| Canonical-copy resolved in ghost-sync.py header | PASS | ghost-sync.py lines 6-17 |
| Ghost not touched (read-only doc fix) | PASS | only docstring/comment edited; no SQL/logic change; parse OK |
| M1 work preserved (Claim Gate, Quote Provenance) | PASS | neither file in edit set |
| No other skill files touched (by me) | PASS | edits isolated to 5 owned paths |

---

## Logged observations (noticed, NOT fixed per surgical-only rule)

1. **`swipe-files/property-sg/` has no `pages/` dir and no per-ad JSON** — only `avatar-registry.json` + `research-pool.json`. If the per-ad JSON is also gone (not just the SQLite), the regeneration path I documented (`rebuild_ads_db.py` walks per-ad JSON) would have nothing to seed from, and Ghost Postgres would be the ONLY surviving copy. Worth confirming whether the durable JSON seed still exists somewhere (Ghost export? git history?) before relying on the regeneration path. Did not investigate further — out of scope.
2. **Pattern-tagging needs a schema field to be machine-usable.** I flagged in the SKILL.md that `swipe-files/schema/ad.schema.json` would need a `pattern` field under `enrichment` before tags can be stored structurally, but did not add it (not an owned file, and adding a schema field is more than a doc change).
3. **`ad-library-scraper` execution recipe still references `mv …draft.md …md` HITL approval** while the SKILL body Phase 6 describes inline-display approval — minor doc inconsistency, pre-existing, left alone.
4. **`/ads:concepts <project>` appears in the SoT hand-off message (SKILL line ~256)** but the routing-overrides note says `/ads:concepts` is a dead command replaced by intent-routed `ad-concept-engine`. Pre-existing stale reference in a part of the file I did not own; logged, not fixed.
