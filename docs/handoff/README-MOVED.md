# Moved to docs/handoffs/

This folder (`docs/handoff/`, singular) was merged into `docs/handoffs/` (plural) on 2026-06-11. Two folders for the same thing was a navigation trap. `docs/handoffs/` is now the only home for engineering/system handoff docs.

What moved: `2026-04-24-copywriting-os-phase-1.md` is now at `docs/handoffs/2026-04-24-copywriting-os-phase-1.md`.

Where to put new handoffs:

- Engineering / system / pipeline handoff docs go in `docs/handoffs/`.
- Session and client handoffs go in `_handoffs/` at the repo root (see `docs/system-rules/session-end-protocol.md` for the convention). The two are different: `_handoffs/` is session memory, `docs/handoffs/` is durable engineering reference.

This pointer stays so old links and muscle memory still land somewhere useful.
