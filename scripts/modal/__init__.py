"""Local scripts.modal package — Modal serverless metrics + sheets helpers.

The empty __init__.py ensures this resolves to the local package rather than the
pypi `modal` (modal.com serverless) package when sys.path inserts this parent.
"""
