# Ad Concept Engine — Corrections

> Append corrections here: `- YYMMDD | what was wrong → what was right | context`

- 260406 | Em-dashes used in headlines → Replace with full stops or commas | Overused-ai-patterns.md strictly prohibits em-dashes
- 260406 | "Here's the honest answer" opener → "The honest answer" | "Here's" is a flagged AI revelation hook pattern
- 260406 | "The secret wealth" framing → "The hidden wealth" | "Secret" revelation hooks are AI-pattern adjacent
- 260406 | Mortgage reframe angle (#3) dismissed buyer fears as wrong → Validate fears, then reframe | Grok + Perplexity both flagged: telling scared buyers their fear is wrong reads as salesman gaslighting
- 260406 | "Mortgage" as word alienates devout Muslim audience → Use "monthly commitment" or "financing" | Riba (interest) prohibition means "mortgage" is theologically loaded for Malay-Muslim families
- 260406 | Phone plans comparison angle (#6) shamed anxious buyers → Avoid judgment framing for anxious personas | Both external validators flagged: shaming people who are already overwhelmed triggers defensiveness, not curiosity
- 260406 | Agent brag angle (#9 "22 years, 729 families") → Reserve for retargeting only, not cold traffic | Cold audiences don't care about your history until they trust you're solving their problem
- 260417 | Scope boundary with source-of-truth skill not enforced → This skill OWNS the DCT tracker + CREATIVES/COPY sheet writes; source-of-truth OWNS strategy (avatars, angles, hooks, messaging). Non-negotiable. | In neezanizam run, DCT creative specs were wrongly placed in source-of-truth-draft.json and its sheet-writer was wrongly wired to write CREATIVES + COPY tabs. Fix: ad-concept-engine's output convention `clients/<project>/campaigns/dct-YYMMDD/dct-tracker.json` is authoritative. **Gap flagged:** `scripts/ad_concept_sheet_writer.py` does not yet exist — needs building under this skill. It should (a) read the dct-tracker.json, (b) take a pre-write snapshot of the CREATIVES + COPY tabs, (c) write CREATIVES strategy columns only (BATCH/FORMAT/AD/MARKET AWARENESS/MARKET SOPHISTICATION/ANGLE/PERSONA — never metric cols), (d) write COPY rows (STATUS/COPY 1/COPY 2/HEADLINE 1/HEADLINE 2), (e) mirror the HITL preview pattern of `scripts/source_of_truth_sheet_writer.py`. Until built, CREATIVES + COPY tab population remains manual.

- 260418 | Pointer source for angle data updated → ad-concept-engine should now read `clients/<project>/angles/wave-N.md` (current wave) instead of root `clients/<project>/angles-hooks-library.md`. Reason: source-of-truth split execution-adjacent strategy into the angles/ folder (260418 architecture decision) so SoT stays stable across waves. Update SKILL.md Phase 1 input contract: `angle_source_file = clients/<project>/angles/wave-{current_wave}.md`. The angles/ folder also contains hook-library.md (master append-only hook bank with anti-pattern log) and iteration-log.md (wave history).

- 260418 | Output schema is incomplete vs the comprehensive ad concept spec — 5 high-leverage gaps logged for v2.1 (not urgent, plan only): (1) Add top-level `strategic_analysis` section to dct-tracker.json with 5 subsections (market_opportunity / positioning_recommendation / message_strategy / channel_strategy / roadmap) — generated by Phase 4.5 via `researcher` agent; (2) Expand headline pool from 2 → 5-7 per concept with explicit `hook_pattern` enum (Question / Identification / Mechanism / Claim / Data-led / Testimonial / Contrarian) — enforce pattern diversity per batch; (3) Add discrete fields to each creative: `key_desire_pain_point`, `visual_concept_type` (Picture of benefit/problem/product/in action), `ad_structure_type` (5 structures enum), `visual_proof_elements`, `call_to_action.specific_next_step + cta_pattern`, `urgency_element`, `objection_handling[].objection + counter`, `why_this_will_work` (per-concept research rationale); (4) Introduce explicit `positioning_strategy` enum on each angle (New mechanism / Superior product / Identity marketing) — drives visual approach choice; (5) Build `scripts/ad_concept_sheet_writer.py` with expanded CREATIVES + COPY columns to match new schema (HEADLINE 1-7, CTA, URGENCY, OBJECTIONS columns added). Effort estimate: ~15-20h. Impact: transforms skill from "creative batch producer" to "strategic advertising platform." Reference: full audit at /tmp/claude-501/.../tasks/a1ec247766fd01183.output (260418 audit).

- 260418 | Phase 2 lumped video and static creative into a single `format` field — UGC/Founder/VSL got one-line creative direction notes, not enough for production. → SPLIT Phase 2 into Phase 2a (Hooks for Static/Carousel: text-on-image hook + visual_concept_type + image prompt) and Phase 2b (Briefs for UGC/Founder/UGC Testimonial/Demo/VSL: full 6-scene breakdown + performer notes + audio + graphics + technical specs + timing map per `references/video-brief-template.md`). Each batch's `creative_type` field in dct-tracker.json records which path: `"hook"` or `"brief"`. Phase 2a addresses Picture of benefit/problem/product/in action enum (gap #3 above closes for static side). Phase 2b creates the missing video brief deliverable. Headlines + ad copy + CTA still produced for both paths.

- 260418 | Phase 3 only routed Phase 2a hooks to `image-generation`. After Phase 2b briefs were added, video creative had no execution handoff. → SPLIT Phase 3 into Phase 3a (Image execution via `image-generation` for `creative_type: "hook"`) and Phase 3b (Video execution via `video-director` for `creative_type: "brief"`). Phase 3b supports two paths: AI-generated video (Sora 2 Pro / Kling / VEO via Vertex AI) OR human-creator brief handoff (brief markdown is the deliverable, sent to UGC creator/founder/editor). Route between AI vs human is HITL-decided at Gate 2.

- 260418 | Multi-product readiness — `references/sophistication-creative-map.md` had property-only examples (HDB / RAM / 729 families). → REWROTE all L1-L5 example sections to include 6 product-type examples per Schwartz level (ecom / SaaS / service / info / agency / property). All filtered examples now route off `product_type` set in `clients/<slug>/offer.md` Product Classification block. Headline structure table, CTA pressure table, text-on-image hooks table all updated to multi-product examples.

- 260418 | Static variant quality bar was implicit — variants came out generic / AI-looking / unclear on the feed. → CREATED `references/high-converting-static-brief.md` with 9 non-negotiable rules (unique variants, SG ethnic distribution, real-not-AI faces with named photographer references, headline clarity on image, bridge line when needed, editorial/documentary/cinema aesthetic not stock-ad look, factually correct info, gut-punch emotional weight). Wired into Phase 2a as MANDATORY load. Origin: Jerel's 260418 brief during neezanizam DCT001 generation — bar is "persona feels physically ill if they scroll past." Applies globally, not neezanizam-only.

- 260418 | `scripts/ad_concept_sheet_writer.py` gap (flagged 260417) → BUILT. Generic, client-agnostic. Reads `clients/<slug>/metrics-config.json` + `clients/<slug>/campaigns/<campaign>/dct-tracker.json`. Supports `--dry-run`, `--client`, `--campaign`, `--batches DCT001,DCT002` filter. Mirrors `source_of_truth_sheet_writer.py` HITL pattern: snapshot → preview → confirm → write → post-write snapshot. Writes CREATIVES strategy columns (BATCH / STATUS / FORMAT / AD / MARKET AWARENESS / MARKET SOPHISTICATION / ANGLE / PERSONA / CANVA LINK if column exists) + COPY rows (STATUS / COPY 1 / COPY 2 / HEADLINE 1 / HEADLINE 2). Never touches metric columns (CTR / CVR / CPA / CALLS / SPEND / DURATION). Prerequisites for any client: (1) service account email granted Editor access to client's sheet, (2) `metrics-config.json` has `creatives` + `copy` tab entries. Corrections.md gap now closed.

- 260418 | Image prompt storage convention locked → Each variant's full Nano Banana 2 JSON lives at `clients/<slug>/campaigns/<campaign-slug>/image-prompts/<batch>-<variant>.json`. The `dct-tracker.json` variant entry references the file via `image_prompt_file` field, never inlines the full JSON. Keeps tracker readable + prompts editable per-file.

- 260420 | Assembled dct-tracker.json with 3 creatives[] entries (one per visual variant) fanning out to 3 CREATIVES + 3 COPY rows → should have ONE creatives[] entry per DCT since angle/persona/awareness/soph/copy are identical across visual variants | CREATIVES tab + COPY tab both track strategic bet (1 row per DCT). Visual variants (S1/S2/S3) are a property of the single row, tracked as visual_variants[] sub-array with per-variant image prompts + Canva links. Rule: one DCT = one angle = one Ad Set = ONE CREATIVES row + ONE COPY row. Image execution count does NOT multiply sheet rows.

- 260420 | Output structure: 2 files per rank (ad_prompt.md + image_prompt.md in separate rank/ folders) → ONE combined file per rank (rankN.md flat) | User directive: bake text into image-gen prompt by default (modern diffusion models can render text), provide Canva fallback section below for when model butchers typography. Redundant Meta primary text + CTA sections REMOVED from the creative file (those live in halbert-copy.md now, not duplicated per rank).
