# Headline Bank — Corrections

> Append corrections here: `- YYMMDD | what was wrong → what was right | context`

- 260418 | Skill created | Scaffolded from user's Static Ad Headline Bank Prompt. First response MUST be the mass-desire question verbatim. Output to `clients/<project>/angles/wave-<N>-headline-bank.md`. Downstream consumer: ad-concept-engine Phase 2a Meta headline picking.

- 260420 | Wrote one COPY tab row per visual variant (3 rows for DCT2-S1/S2/S3) → should write ONE COPY row per DCT regardless of visual variant count | Halbert output is DCT-level (2 headlines + 2 copies once per angle). Visual variants share the SAME copy/headlines. Sheet tracks strategic bet, not execution variants.
